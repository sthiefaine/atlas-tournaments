# CLAUDE.md — passation

Document de passation pour Claude Code. Il dit ce qu'est le projet, où sont les choses, ce qui est vrai aujourd'hui et ce qui ne l'est pas. Quand il contredit `BRIEF.md`, c'est `BRIEF.md` qui a raison.

## Mise à jour — la carte à gauche, la colonne à droite, et un seul vocabulaire (8 septembre 2026, nuit)

Trois demandes du propriétaire : « je veux la carte à gauche sur PC et un panneau à droite » ; « j'aime pas du tout le style de l'UI, faut plus pousser ça vers le jeu » ; « et ni le style du fil de la campagne ». Plus, en cours de route, deux retours sur la prévision de duel. Il a demandé de consulter **un ado et un joueur invétéré d'Advance Wars** : deux personas ont lu le code de l'interface — pas l'écran, consigne — et rendu deux avis qui convergent sur l'essentiel.

1. **Le HUD n'est plus une surcouche, c'est une mise en page** (`hud-html.ts`, §rail). Onze panneaux flottaient sur une toile plein cadre, dont trois couvraient en permanence des cases jouables. Au-delà de 1000 × 560 de **conteneur** (`railTient`, pure et testée), la carte tient la gauche et une colonne de 340 px tient la droite. Ce n'est pas un panneau posé sur l'image : le conteneur rend sa place à la toile par une marge (`--rail-l`, posée par `majRail`), et `render3d/scene.ts` **mesure la toile et non le conteneur** — sans quoi la caméra aurait continué de cadrer la carte derrière la colonne, avec un `versEcran` faux de la largeur du rail. Sous le seuil, les deux zones passent en `display:contents` et **rien ne change** : chaque panneau se repositionne exactement comme avant.
2. **La règle qui répartit : ce qui commente une case reste sur la case.** `ZONES` (exportée, testée) : le menu d'ordres, la prévision de duel, le curseur, la caméra, l'attente et l'annonce restent sur l'image ; la journée, les fonds, l'unité regardée, la météo, le pouvoir et la fin de tour vont dans la colonne ; les modales ne relèvent d'aucune des deux, leur voile couvre les deux. `ancrer()` calcule sur la largeur de **l'image** (`racine.clientWidth - largeurRail`), les scènes se bornent à l'image (`--rail-l` lu par `.atlas-scenes`), et la barre de mission descend de `z-index:15` à `4` — elle était peinte **par-dessus** l'écran de combat et le voile de production.
3. **Le premier gain n'est pas esthétique** : le panneau d'unité ne s'efface plus quand le menu d'ordres s'ouvre. Les PV, les munitions et le carburant quittaient l'écran à l'instant précis où l'on choisit entre attaquer et capturer. Ce n'était pas un manque de place, c'étaient deux panneaux qui se disputaient un coin.
4. **Quatre informations existaient dans l'état et n'étaient affichées nulle part**, toutes signalées par le joueur d'Advance Wars et vérifiées dans le code : le **compteur de capture** (`hud.capture` était dans les chaînes et appelée par personne ; le seuil est lu au moteur, il double sur un QG et un désaffecté), le **revenu** de la prochaine journée (`revenuParTour`, isolée dans le moteur pour ne pas être recopiée), le **compte de bâtiments** mien contre sien, et le **super pouvoir** — mécanique complète du moteur au splash, injoignable parce qu'un seul `'normal'` était écrit en dur dans `surClic`. Au passage, le bouton de pouvoir avait un **seuil faux** : actif à jauge pleine seulement, alors que la jauge se remplit jusqu'au prix du super et que le pouvoir normal coûte moins ; c'est le verdict de `verifierPouvoir` qui décide désormais, passé par `VueJeu.pouvoirs`.
5. **La prévision de duel maigrit**, sur retour du propriétaire — « je ne comprends pas la riposte, ça fait trop d'informations à l'écran ». Le terrain, ses étoiles, la part retirée, les mots « Coup » et « Riposte » et la note d'explication s'en vont : deux figurines, deux jauges, deux nombres, et un bandeau qui ne dit plus que le **notable** (cible hors jeu, tir sans riposte). Le terrain se lit dans le panneau d'unité, qui ne s'efface plus. C'est l'inverse de la décision du 8 septembre au matin, et c'est le même homme qui tranche : ce qui manquait alors était l'explication, ce qui gêne maintenant est sa longueur.
6. **`FACTEUR_RIPOSTE = 0,80`** (`doc/04` §5.1). Sur la scène qu'il a rapportée — deux infanteries à 8 PV, la sienne sur route, l'autre en ville —, son coup retirait 20 points internes et la riposte lui en rendait **21** : les trois étoiles de la ville effaçaient l'avantage de l'initiative, et les deux camps affichaient « 8 → 6 ». La riposte garde 80 % de sa force : il sort à 7, la ville à 6, ce qui était sa demande. **Le facteur a été choisi par la mesure** : 20 parties de plaine, graine 1, catalogue 6, pondérée contre agressive — sans facteur **60/40**, à 0,80 **50/50**, à 0,70 **15/85**, où une riposte trop faible récompense tant l'attaque que le jeu n'a plus qu'un plan. 0,70 donnait le même 7-6 à l'écran : c'est la simulation, pas le duel, qui a tranché. Les matchs s'allongent de trois journées médianes (34 → 37), aucun ne finit au chronomètre. `VERSION_MOTEUR` passe à **5** ; l'IA lit le facteur au lieu d'en garder une copie ; `verifier:campagne` reste 6/6, rejeu conforme.
7. **Le fil de la campagne devient un itinéraire.** Il était un article — colonne de prose à 1040 px, six entrées « 01 » à « 06 » séparées par des filets gris, bouton pilule vert, liens soulignés, glossaire en pied — et surtout **le seul écran du jeu sans fond à lui** : il héritait du `body` de Tailwind, donc blanc en thème clair et suivant le réglage du système. Six stations sur une ligne, un trait qui se remplit derrière soi, un **dossier à la fois**, trois états par la peinture et jamais par une opacité sur du texte. Le calcul sort du composant (`campagne/itineraire.ts`, pur, testé), comme `parties-libres.ts` le fait pour `/jeu`.
8. **Un seul vocabulaire de boutons.** Il y avait `border-radius` en 2, 5, 6, 12, 13, 14, 24 px et 50 %, trois anneaux de focus, et des boutons plats à côté de boutons en relief. La règle du HUD est généralisée : **un bouton a une épaisseur** — bordure basse plus sombre que sa peinture, qui s'écrase de deux pixels à l'appui —, le coin se coupe au lieu de s'arrondir, l'ombre est dure. L'anneau de focus est unique et sa couleur suit la **surface** : le signal ne se voit pas sur le papier crème d'un briefing.

**Non fait, et les deux personas le demandaient** : le cyclage des unités non jouées (`recentrer` va sur la sélection, sinon sur la première du tableau — pas sur la prochaine à jouer) ; la **zone de danger** rendue découvrable (le calcul existe, le geste est un double-clic que rien n'annonce) ; le coût du chemin pointé pendant la visée ; un bilan de fin de match ; le menu de production sorti de sa modale plein écran vers un panneau ancré au bâtiment ; le relevé fantôme sous le curseur, qui est le prix à payer pour une colonne à huit cents pixels de la main. **Non fait, autre** : `/campagne` embarque toujours les 379 chaînes d'interface parce qu'il appelle `t()` côté client, contre la convention du dépôt — `menu-campagne.tsx` montre comment faire. **Non vérifié, par consigne** : tout le visuel — la tenue de la colonne de 340 px avec les longueurs françaises, la lisibilité des trois états de station, et si la prévision réduite en dit encore assez.

## Mise à jour — l'embuscade trop large, et le démarrage qui gelait la page (8 septembre 2026, soir)

Deux retours du propriétaire en jouant : « j'allais tout droit, j'ai eu une unité à ma droite et ça m'a quand même coupé ma course » ; et « en WebGPU l'animation de l'accueil est très longue à s'initialiser comparé à WebGL 2 — corrige ça, et améliore le chargement des niveaux et l'écran de chargement ». Deux agents sur des lots disjoints, l'embuscade faite à la main.

1. **L'embuscade ne se déclenche plus qu'au contact** (`doc/04` §10). La règle disait « sur le chemin **ou à côté** », et le code cherchait une unité cachée à `manhattan <= 1` de chaque case du trajet : on passait devant une silhouette sans jamais la toucher, et le tour s'achevait là. Une unité cachée n'interrompt désormais la marche que si elle occupe **une case du chemin**. `tests/engine/embuscade.test.ts` rejoue les deux cas, dont exactement celui du propriétaire.
2. **Le chargement d'un niveau montrait une page vide**, et c'était deux fautes cumulées, mesurées : `next/dynamic(..., { ssr: false })` n'avait **aucun `loading`**, donc le corps de la page restait littéralement vide — 4,2 s de néant — jusqu'à l'arrivée du module ; et l'écran de chargement s'effaçait quand `monterJeu` rendait la main, soit **3 à 5 s avant la première image**. Sur l'accueil, `Vitrine` partait d'un état « indécis » et ne rendait donc **rien** côté serveur : **8,3 s de fond vide**, le SVG étant pourtant sérialisé dans la charge (20 ko payés pour rien). Après : l'écran de chargement est **dans le HTML du serveur**, visible dès 124 ms, et il s'efface **après** la première image dessinée (1,47 s) ; l'accueil montre son plateau à plat dès 545 ms et bascule sur la 3D en 7 ms, quand elle a réellement dessiné. Le fondu de 600 ms est supprimé — c'était lui qui montrait deux jeux à la fois. Un effet React qui **démontait et remontait tout le moteur** juste après la première image a été trouvé et corrigé au passage (700 ms de noir).
3. **L'écran de chargement dit quatre choses vraies** (`chargement.tsx`, `etapes-chargement.ts`, `render/jeu.ts` : `etapeChargement`, pure et testée) : module arrivé, plateau bâti, moteur initialisé, première image dessinée. Aucune jauge qui avance seule — un ruban indéterminé animé en `transform`, la seule chose que le compositeur bouge encore pendant que le fil principal traduit les nuanceurs. La peau est **fabriquée et montée en premier**, avant catalogue et rejeu : l'initialisation asynchrone du moteur se recouvre avec la lecture de la sauvegarde au lieu de s'y ajouter. Survoler un lien de partie précharge son module (`precharger.ts`).
4. **La première image ne gèle plus la page** (`doc/10` §9.5). Deux remèdes. **Moins de programmes** : sous WebGPU c'est le nombre de **clés distinctes** qui coûte, et three sépare sur deux différences qui n'en sont pas — un nombre réduit à « nul ou non » dans la clé alors que `metalness` et `emissiveIntensity` ne sont que des uniformes (WGSL identique, vérifié en comparant les deux nuanceurs construits), et l'ordre d'insertion des attributs de géométrie. `programmes.ts` corrige les deux : `premier_contact` passe de **39 à 31** programmes, une figurine de 11 à **4**. **Et un préchauffage par lots** : `compileAsync` fonctionne — le §9.4 disait le contraire —, à trois conditions trouvées par la mesure (éteindre `frustumCulled`, préchauffer sur la cible en demi-flottants de l'image vraie, éteindre la passe d'ombres, dont le `ShadowNode` lève au milieu de la compilation). Résultat : la première image bloque **250 à 280 ms au lieu de 840 à 894**, et le préchauffage prend ~700 ms **en huit tranches** dont la plus longue tient 250 ms. **Le total ne baisse pas** : ce qui change est qu'aucun bloc ne dépasse le quart de seconde et que la page répond pendant tout ce temps.


5. **Le plateau à plat n'est plus une attente** (demande du propriétaire, « c'est quoi ce truc dégueulasse en 2D, j'en veux pas ») : le SVG de l'accueil ne paraît qu'en **repli** — animations réduites, réglage du joueur, moteur absent, ou renoncement au bout du budget —, jamais pendant que la 3D démarre. Montré en attente, il se lisait comme un autre jeu, plus pauvre, servi avant le vrai. Le fond de l'attente est la nappe sombre de l'écran-titre, dont le titre et le menu viennent du serveur.
6. **`batir()` ne gèle plus la page non plus** (`doc/10` §9.6). C'était le dernier gros bloc du chargement, et il n'avait rien à voir avec les nuanceurs : 120 ms pour `premier_contact`, dont **97 pour le plateau** et 82 de synthèse de toiles. Trois remèdes, **sans qu'une texture change d'un octet** — 336 empreintes prises avant et après sont identiques, et deux tests rejouent la formule d'origine pour le prouver. Les **boucles** : bruit de valeur rendu **séparable** (deux lectures par pixel au lieu de quatre, lignes intermédiaires en double précision, sinon les bits changent), tables d'index et racine carrée dans `normalesDonnees`, mélanges de couleurs à plat — `mel` allouait jusqu'à quatre tableaux **par pixel**, un demi-million par atlas de voies. La **mémoire** : ce sont les **pixels** qui sont gardés, jamais les textures (chaque plateau fabrique et libère les siennes, sinon un `dispose()` tuerait le montage suivant), cache par document borné à 24 toiles, 5,5 Mo ; et `CalqueUnites.dispose()` **vidait** deux caches pourtant au niveau module, si bien que chaque montage repayait la fusion des sept rôles de chaque unité. Les **tranches** : `batir()` devient `ouvrirChantier()`, sept tranches de toiles puis plateau, décor, unités, chacune dans sa tâche. Résultat : **120 ms d'un bloc → 66 ms en dix tranches dont la plus longue tient 9,7 ms**, et **7,8 ms** au second montage de la même page. Mesuré sous Node ; le navigateur paie environ le double, à confirmer au banc.

**Ce qui reste, dans l'ordre du gain** : la passe d'ombres (12 à 17 programmes, ~250 ms), impossible à préchauffer sans le matériau que `ShadowNode.js` garde en variable de module ; les treize lots instanciés du décor, dont chacun coûte son programme parce que r170 met l'`uuid` de la maille dans la clé dès que le compte dépasse un — le remède est un nœud TSL d'instanciation partagé, à la manière du masque d'équipe ; et **`batir()` lui-même**, désormais le plus gros bloc du chargement de l'accueil (~490 ms de synthèse de textures et de fusion de géométries), sans rapport avec les nuanceurs. `webgl2Disponible()` garde encore la porte de la vitrine alors que le moteur est WebGPU : un appareil WebGPU sans WebGL 2 n'aurait jamais l'attract. **Non vérifié, par consigne** : la mise en page de l'écran de chargement, et si la coupe franche du plateau à plat vers la 3D se lit bien — c'est le seul point de goût de ce lot.

## Mise à jour — la flèche, et « ça lag » : deux spécialistes, une mesure (8 septembre 2026)

Le propriétaire, après le portage : « j'ai l'impression que ça lag, et je n'ai pas la flèche de direction quand je clique sur une autre unité ». Deux agents, l'un spécialiste de three sous WebGPU, l'autre sous WebGL 2 et de la performance, sur des lots disjoints. **Rien regardé à l'écran** — le volet du navigateur est masqué, la page n'y compose aucune image.

1. **La flèche : la parade du 7 au soir était fausse, et le défaut était structurel** (`doc/10` §10). L'attribut témoin **alterné** comptait les échanges de géométrie, alors que le moteur ne connaît que celles qui sont **dessinées** : cliquer sur une unité pose un chemin d'une case, donc une flèche vide et invisible, et l'aller-retour décalait les deux comptes d'un cran — un survol sur deux réaffichait la flèche d'avant, figée, reconstruite depuis un tableau libéré. Reproduit hors navigateur en rejouant `getGeometryCacheKey`. Deux états ne pouvaient pas suffire : une maille paraît dans **plusieurs passes** (principale, ombre — qui ne tourne pas à chaque image —, normales du GTAO), chacune avec son objet de rendu et son rythme. **La règle est devenue : on n'échange plus la géométrie de ce qui change souvent.** `render3d/maillage.ts` porte un `TamponMaille` — attributs préalloués qu'on remplit, `setDrawRange`, `needsUpdate` — et il n'y a plus rien à invalider. Surbrillances et, au terrain, socle, grille, voies et ponts sont au tampon ; le sol et l'eau gardent l'échange avec un témoin **monotone**.
2. **Et c'était une part du lag.** Mesuré sur 300 survols : **675 objets de rendu rebâtis avant, 3 après**. Comme `Nodes.delete` évince l'entrée de `nodeBuilderCache` dont le compteur d'usage tombe à zéro, l'ancien code **reconstruisait le WGSL et créait un pipeline à chaque mouvement de souris**. Il reste 9 clés de géométrie pour toute la couche, toutes créées aux premières images.
3. **Le portage n'a rien coûté par image, au contraire** (`doc/10` §9.4). Même plateau, même protocole, canevas 2560 × 1600 : WebGPU rend une image en **10,2 ms** derrière la barrière GPU et coûte **2,5 ms** de JavaScript ; son repli WebGL 2 met **25,0** et **5,9**. Le choix automatique du dos est le bon. **Ce que le portage coûte est ailleurs, et c'est le seul vrai défaut : la première image bloque le fil principal environ une seconde** (1,6 s pilote froid), le temps de traduire TSL en WGSL et de créer les pipelines, quand l'image suivante coûte 2,5 ms. Un gel d'une seconde à l'instant où le plateau apparaît. **Le poids du premier chargement, lui, est revenu** : depuis que la toile est chargée côté client seulement (`toile-client.tsx`), `/jeu/[scenario]` pèse **1,3 ko sur 105 ko** au premier chargement au lieu de 330 sur 500 — le moteur arrive après, pendant que la page est déjà là. `compileAsync()` **ne le résout pas** : il projette la scène contre un tronc de vue jamais renseigné avant la première image, ne trouve rien, et laisse l'écran noir — essayé, retiré, expliqué dans `scene.ts`.
4. **Deux mesures étaient fausses au moment où on les lit.** `msParImage`, moyenne glissante, était tirée à 9,8 ms par la première image pendant une centaine d'images : c'est désormais la **médiane** des trente dernières, et elle rend 1,5 à 2,2 ms. `msCadence` — la seule mesure qui compte le processeur graphique en jeu sans barrière — n'était prise que la chaîne allumée : elle l'est maintenant toujours, et se lit dans `MesuresRendu.msCadence`.
5. **`composeur: false` est un verdict, pas une panne** : `msCalibration` vaut 10,2 ms à 4,1 Mpx, 8,3 à 1,0 Mpx et 4,9 à 0,44 Mpx, où la chaîne s'allume vraiment. Le seuil de 7 ms tombe au milieu de la plage utile.
6. **Six limites de r170 sont maintenant écrites dans le code**, les trois du 7 septembre plus : `updateRanges` écrit toujours à l'offset 0 sous WebGPU et corromprait un téléversement partiel ; le **nombre d'instances** d'un `InstancedMesh` sous 1000 est figé dans le WGSL au premier rendu, donc un lot dont le compte change se **rebâtit** ; un `count` de zéro dessine **une** instance, d'où `allumer(lot, n)` dans `decor.ts`. Et un piège de mesure : **`triangles` ne se compare pas d'un dos à l'autre**, three appelant `Info.update` avec la mauvaise signature côté WebGL.

**Non fait** : le gel de la première image (réduire le nombre de matériaux distincts, ou dessiner d'abord une scène pauvre) ; une fuite de six états WGSL par marée, un `InstancedMesh` jeté ne défaisant rien côté moteur ; la boucle qui ne dort jamais en partie. **Non vérifié** : la cadence réelle à l'écran et l'aspect de l'image, faute de volet affiché.

## Mise à jour — les dégâts : une échelle, un terrain qui compte (8 septembre 2026)

Le propriétaire, en jouant : deux infanteries pleines, aucun pouvoir, l'une sur route l'autre en forêt ; après l'échange, 5 PV d'un côté et 8 de l'autre. « C'est beaucoup trop, et ce n'est pas logique. » Trois agents en parallèle sur des lots disjoints — la formule, la table, l'écran.

1. **Le 5-8 n'était pas un bug**, et le test le prouve (`tests/engine/degats.test.ts`, « la scène du propriétaire ») : l'attaquant frappe intact, le défenseur **riposte avec ses PV restants**. C'est la mécanique d'Advance Wars, inchangée. **Le vrai défaut était ailleurs, et il était réel** : `Fterrain = 1 − 0,05 × E × (pvCible/10)` faisait que la forêt et la route ne se distinguaient pas d'un PV affiché (55 contre 50), et la protection **s'évaporait au moment où elle servait** — une unité à 3 PV en forêt n'avait plus que 3 % d'abri.
2. **Deux constantes nommées** (`src/engine/regles/combat.ts`, `doc/04` §5.1). `REDUCTION_PAR_ETOILE = 0,10`, sans plus dépendre des PV de la cible : la forêt retire 20 %, la montagne 40 %, à tout moment. `ECHELLE_DEGATS = 0,65` : le rythme du jeu, **séparé de la table** — `content/degats.json` dit qui bat qui, l'échelle dit à quelle vitesse. Résultat, exactement la cible demandée : une infanterie pleine qui frappe une infanterie pleine lui laisse **8 PV en forêt, 7 sur route**. **Pourquoi 0,65 et pas 0,60**, qui se lit pareil : la falaise est à la base **15**, la bande de grignotage contre un blindé lourd — `arrondi(0,65 × 15)` vaut 1 PV affiché, `arrondi(0,60 × 15)` vaut **rien**. À 0,60, huit des quinze unités qui ont le droit de tirer sur un char lourd ne lui ôtaient aucun PV : une attaque légale qui ne fait rien est une règle incompréhensible. À 0,65, tous les contres tiennent en trois coups au lieu de quatre.
3. **L'IA recopiait la formule**, et sa copie a menti dès que l'originale a changé : `degatsAttendus` (`ai/evaluation.ts`) et `menace` (`ai/strategies/ponderee.ts`) portaient chacun leur `1 − 0,05 × E × pv/10`. Mesuré : la pondérée tombait de 65/35 à **15/85**. Le moteur exporte désormais `ECHELLE_DEGATS` et `facteurTerrain(etoiles)`, et personne ne recopie plus rien — c'est la leçon des quatre listes recopiées du 7 septembre, appliquée à une formule.
4. **Un contre manquant, trouvé par la carte des 302 couples** : le char lourd n'avait de réponse que lui-même (4 coups) en catalogue 4, et rien d'autre ne lui ôtait plus d'un PV. `roquettes → char_lourd` passe de 55 à **70** (miroir dans les deux fichiers), et la contrainte 2 du §13.3 — « au moins un contre à ≥ 70 » — est **étendue aux 24 unités** dans `tests/schemas/contenu.test.ts` : la table du §8 s'exemptait de sa propre règle.
5. **`VERSION_MOTEUR` passe de 3 à 4.** Une partie enregistrée avant diverge dès la première attaque : les sauvegardes du 3 sont déclarées périmées et le joueur repart d'une partie neuve. Rien ne casse au-delà du match en cours.
6. **L'écran dit enfin d'où vient le chiffre.** La prévision porte le terrain de chaque camp avec ses étoiles **et la part retirée** (« Forêt ★★☆☆ −20 % », lue par `facteurTerrain`, jamais recalculée), l'attaquant étant jugé sur le terrain de son **arrivée** ; les deux lignes sont étiquetées **Coup** et **Riposte**, avec la note « La riposte part à {n} PV, après le coup » — la raison exacte de l'asymétrie qui a fait réagir le propriétaire. Sans riposte, le panneau le dit au lieu d'afficher « −0 PV ». Les chiffres flottants nomment la riposte (`rolesDesChiffres`, pure), et la fiche gagne « Le terrain la protège » (paliers d'étoiles) plus une note : une unité entamée frappe moins fort.

**Mesures** (20 parties, graine 1, catalogue 6, pondérée contre agressive) : plaine **55/45**, médiane **32,5** journées, **0 partie sans vainqueur** (avant l'échelle : 40/60, médiane 27,5, 5 à la limite). Les parties sont environ un cinquième plus longues, et elles se **décident** toutes. `verifier:campagne` **6/6**, rejeu conforme.

**Non fait / à surveiller** : plus aucune unité pleine n'est mise hors jeu d'une seule salve (la plus grosse case de la table, 130, rend 85 PV internes) — les contres durs sont devenus des contres en deux coups ; le **soin des bâtiments** (2 PV affichés par tour) rend une infanterie en ville très difficile à déloger avec une seule unité, et c'est le prochain chiffre à regarder ; le geste `chiffre` de la partition devrait porter son rôle (`coup` ou `riposte`) au lieu que le HUD le déduise de l'ordre des gestes ; `prevoirDuel` ne dit pas *pourquoi* il n'y a pas de riposte. Tout le visuel reste non vérifié, par consigne.

## Mise à jour — WebGPU, la vision et le terrain, l'ordre en deux temps, les hautes herbes (7 septembre 2026, nuit)

Trois demandes du propriétaire dans la foulée du catalogue 6, faites par sept agents en deux vagues et vérifiées par du code : `typecheck`, `lint`, `npm test`, `verifier:campagne` 6/6, `build`. **Rien regardé à l'écran.** Depuis cette nuit, le propriétaire demande **le commit et le push sur `main` à chaque étape, sans co-auteur**.

1. **Le moteur de rendu est WebGPU** (`doc/10` §10). `WebGPURenderer` de three r170 (`three/webgpu`), nuanceurs en TSL (`three/tsl`), backend WebGL 2 du même moteur en repli quand `navigator.gpu` manque ou que `requestAdapter()` rend `null` (`choisirBackend`, `forceWebGL`) — r170 lève au lieu de retomber, il faut décider avant de construire. **Un seul three** : le rendu importe `three/webgpu`, les compléments importent `three`, `next.config.ts` aligne les deux (webpack et Turbopack) ; sous Node, `tests/aides/webgpu-en-node.mjs` (préchargé par `scripts/test.mjs`, **après** le crochet de tsx, pour `require` comme pour `import`) fait pareil et fournit les deux globales que le moteur exige au chargement — **lancer un sous-ensemble de tests se fait par `node --import tsx --import ./tests/aides/webgpu-en-node.mjs --test <fichiers>`**, jamais `npx tsx --test` seul (deux copies de three, `instanceof` faux). `creerScene3d` expose `prete` (`await renderer.init()`), `dessiner()` attend, la calibration est asynchrone (`waitForGPU`), `PostProcessing` en TSL (MRT normales, `ao` du GTAO node, vignette, grain, saturation), `mesures.ts` lit `renderer.info`, `mesurer().backend` dit quel dos tourne. Terrain : sol (`nuanceurSol`), eau (`nuanceurEau`) et brouillard (**un** `outputNode` par plateau, lu sur `positionWorld` après `instanceMatrix` : le piège d'instanciation disparaît) en nœuds ; unités : masque d'équipe en un nœud partagé lu par `materialReference`, matériaux GLTF convertis en jumeaux à nœuds à la lecture ; les particules sont un quad instancié (un `Points` fait un pixel sous WebGPU). **Trois limites de r170, écrites dans le code** : `clone()` d'un `NodeMaterial` perd couleur, cartes et rugosité (`clonerMateriau`, `clonerMateriauNoeud`, `doublerMateriau`) ; les couleurs de sommet ne se lisent qu'en `vec3` ; une texture ne change pas de taille (splat, fonds, masque remplacés au changement de carte) ; `polygonOffset` est inerte. Les tests construisent le **WGSL réel** hors navigateur (`tests/render3d/nuanceur.ts`). La vitrine ne s'évalue plus côté serveur (`vitrine-client.tsx`, `ssr: false`) : le moteur suppose un navigateur dès l'import. **Coût** : `/jeu/[scenario]` passe de 96 ko à 330 ko de JS propre (500 ko au premier chargement contre 462) — le moteur WebGPU embarque tout le système de nœuds. `e2e/fumee-3d.spec.ts` n'a pas été lancé (consigne).
2. **La vision et le terrain** (`doc/04` §10, §12.3), tranchés par deux personas — un joueur novateur, un ado —, qui ont dit la même chose : montagne **+3 à pied seulement** (`BONUS_VISION_MONTAGNE`, rien à ce qui vole) ; forêt qui cache **et** bouche la vue (**−1**, plancher 1, `MALUS_VISION_FORET`) ; bâtiment possédé qui voit **1** (`VISION_BATIMENT`, la station radar 5) ; nuit −2 sauf sur un bâtiment à soi, **la ville est éclairée** ; portées des unités inchangées. Sur remarque du propriétaire dans la foulée : **la montagne coupe la ligne de vue** (`ligneCoupee`, Bresenham entre la source et la case ; la montagne elle-même se voit ; qui est sur une montagne ou qui vole voit par-dessus, un bâtiment jamais), et **les herbes sont rares** (presets : plaine 0,06, forêt 0,04, jungle 0,08, côtier 0,05, marais 0,25 — c'est le biome qui décide). **Quinzième terrain, `herbe_haute`** (`G`, « Hautes herbes ») : plaine pour tout, sauf qu'elle cache `pied` et `bottes` au-delà du contact — `Terrain.cacheSeulement`, « l'herbe cache ce qui est plus bas qu'un char » ; dans le glossaire (avec le port), sur le banc, sur les deux cartes libres (taches symétriques), dans l'aperçu raster (touffes) et le paysage 3D (touffes denses, `herbes_hautes`, sol de plaine) ; le générateur la sème en taches (`ratioHerbesHautes`, facultatif, `herbes.ts`, après le bâti et les routes, symétrie exacte, cour du QG nue ; presets `plaine` 0,2, `foret` 0,1, `jungle` 0,15 ; les 24 empreintes tiennent). `verifier:campagne` reste 6/6. Idées des personas **non faites** : un aperçu pointillé de ce que verra l'unité à l'arrivée ; une unité qui tire ou capture repérée jusqu'au tour suivant.
3. **L'ordre en deux temps** (`doc/04` §2). Sous brouillard, « Capturer » était proposé sur une ville qu'on ne voit pas. Suite `{ type: 'puis' }` : on bouge d'abord ; sans embuscade l'unité passe **`deplacee`** (troisième état d'`EtatUnite`) et donne sa suite par un second ordre **sans chemin**, traité comme « a bougé » ; refus `deja_deplacee` pour un chemin ou un second `puis` ; à la fermeture du tour, une suite jamais donnée est perdue. Le contrôleur ne le fait que si une case du chemin est hors de vue ; un chemin visible garde le menu avant (annulable). Une `deplacee` ne s'éteint pas en 3D (`agie` = `agi` ou `produite`). L'IA garde ses ordres atomiques.
4. **Le « ! » d'embuscade.** Heurter une unité cachée arrête la marche sur la dernière case libre et **finit le tour** (`agi`) ; la partition gagne le genre `surprise` (700 ms) après le `glisser` d'un déplacement interrompu, le HUD le joue (un « ! » ancré sur l'unité, ponctuation hors `t()`), le bulletin annonce `hud.embuscade` ; la peau 3D l'ignore (genre inconnu → `null`).

**Non fait / à trancher** : tout le visuel du portage (mélange du sol, eau, brouillard, masque, verre, flocons, décalques sans `polygonOffset`) ; la fumée Playwright sous WebGPU ; l'aperçu de vision ; la taille du bundle.

## Mise à jour — catalogue 6 : le chasseur furtif, et « corrige tout » par cinq agents (7 septembre 2026, soir)

Après le bilan des manques par unité, le propriétaire a demandé de **tout corriger et tout améliorer**, par agents en parallèle, et d'ajouter « un avion qui peut être furtif comme un mirage ». Schémas, moteur et contenu ont été posés d'abord d'une seule main, puis cinq agents sur des lots disjoints du même arbre : moteur-tests-`doc/04`, IA, générateur, rendu-HUD, pages-et-pièces. Vérifié par du code : 1 034 tests (1 sauté faute de base), `typecheck`, `lint`, `verifier:campagne` 6/6 rejeu conforme, 972 specs à jour, `build` vert. **Rien regardé à l'écran, rien commité.**

1. **Le chasseur furtif, 24ᵉ unité, catalogue 6** (`doc/04` §10 quinquies). Trait **`furtif`**, quinzième de la liste fermée : une furtivité **à la demande** — la suite d'ordre `furtivite` bascule `Unite.furtive` (champ optionnel, absent = visible, les états du moteur 3 restent lisibles) ; furtive, l'unité n'est repérée qu'au contact (`cacheeAuContact`), attaque et riposte normalement, reste furtive après un tir, et paie `SURCOUT_CARBURANT_FURTIF` = 3 par tour (`consommationParTour(type, u)`, 8 au lieu de 5). L'unité `furtif` : 20 000, air, mouvement 6, vision 3, 6 munitions, 60 / 1 / 5, `ailes · plateau · [antenne] · 3`, produite à l'aéroport, embarquée par le porte-avions ; 85 sur le mou et l'air, 20 sur le char lourd, 30 sur l'anti-air, **0 sur le sous-marin** (qui garde ses cinq chasseurs) ; touchée par sept viseurs (infanterie 5, méca 8, anti-air 75, missiles air 100, chasseur 85, furtif 55, porte-avions 25 — l'exception écrite du §13.3). Le nom « Mirage » n'a pas été pris : c'est une marque. **Le plafond de 24 du §13.7 est atteint** : la prochaine homologuée en retire une. Les deux cartes libres passent en catalogue 6, les missions restent en 4.
2. **Les trous de règles du bilan sont bouchés.** `transport.ravitaille` : le porte-avions et le camion remettent au plein munitions et carburant de leur cale à chaque début de tour, jamais les PV ; la barge et le transport d'assaut ne font que porter. `debarquer` accepte `passager` et `autres` : **un ordre vide la cale**, un refus annule tout puisque `appliquer` travaille sur une copie. Et une **embuscade**, trouvée par l'agent IA : sous brouillard, une unité cachée sur l'arrivée était un refus `case_occupee`, donc une information donnée au joueur ; le moteur interrompt désormais la marche sur la dernière case libre (`verifierChemin`, `executerOrdre`, `tests/engine/embuscade.test.ts`). `signatureVue` ignorait `furtive` : corrigé, testé.
3. **L'IA** (`src/ai/`, `deplacement.ts` neuf, `doc/02` §3.2) : achats conscients du **potentiel** adverse — ce que ses bâtiments peuvent produire, `POIDS_POTENTIEL` 0,5, `contreAchat` sur les menaces que l'armée ne couvre pas, épargne `TOURS_EPARGNE` 2 —, elle achète enfin chasseurs, furtifs, cuirassés et lance-missiles ; fin du spam de transports (`besoinTransport` : clients réels, îles, places déjà offertes, pertes subies) ; débarquement en un ordre ; furtivité jouée (se cache sous menace, se montre quand le carburant presse, se pose sur un porte-avions qui ravitaille) ; escorte (`RAYON_ESCORTE` 2) ; tir indirect vers la couronne de portée, donc le cuirassé bombarde du large ; **brouillard honnête** (`BROUILLARD_HONNETE`, `adversairesConnus`). Mesures, 20 parties, graine 1, pondérée contre agressive : plaine 35/65 → **40/60**, `limite_journees` 5, médiane 27,5, identique en catalogues 4 et 6 ; bras de mer 30/70 → **60/40** (barges 113 → 39, transports d'assaut 125 → 36, bombardiers 5 → 33, cuirassés 0 → 20, furtifs 38) ; archipel 90/10 → **80/20** mais **20 parties sur 20 au chronomètre** contre 18 : les deux îles saturent et un débarquement à deux meurt, il faudrait une coordination de groupe que cette IA gloutonne par unité n'a pas. Non fait : la chasse au sous-marin (sans mémoire de dernière position, une coque cachée n'existe pas pour une IA honnête), `fusionner`, le drone et le brouilleur tactiques.
4. **Le générateur pose des ports et des radars** (`ParametresCarte.portsParCamp`, `radarsParCamp`, facultatifs 0 à 2, `doc/03` §5, `doc/05` §3). Un port est une case de terre côtière à 3–11 pas du QG dont les images symétriques sont elles aussi côtières, et **tous les ports d'une carte desservent la même mer** (composante d'au moins 4 cases). Quand des ports sont demandés, **le pourtour de la carte est de la mer**, imputé à `ratioMer`, et les chaussées ne le traversent pas — sans quoi 25 graines sur 72 posaient leurs ports, 72 sur 72 après. Radar à 2–10 pas, jamais collé au QG. `MAPGEN_VERSION` reste 2 : 24 empreintes de non-régression prouvent qu'aucune grille d'avant ne change. Deux motifs de rejet neufs, `port_sans_mer` (bloquant) et `ports_isoles` (majeur). `controler` sur une carte côtière générée avec ports : **rejetée**, `avantage_premier_joueur` 67 %, valide sans ports à la même graine — mesuré, non creusé. L'atelier n'expose pas encore ces paramètres.
5. **Le rendu** : ordre « Se cacher / Se montrer » (`hud.se_cacher`, `hud.se_montrer`, glyphe œil barré), état `hud.furtive`, annonces de bascule, figurine furtive translucide (`OPACITE_FURTIVE` 0,45 ; `Materiaux.translucide(origine, opacite)` généralise `terni()`), deux genres de partition `voiler`/`devoiler` (480 ms). **Une fuite corrigée au passage** : le calque dessinait par *case* vue et non par *unité* vue, si bien qu'une furtive ou une unité tapie en forêt hors contact apparaissait sur une case éclairée (`VueInteraction.unitesVues`, `CalqueUnites.maj(…, vision)`). Fiche : la cale (places, types acceptés, « ravitaille sa cale »), la consommation par tour et furtive, les dégâts effectifs à sec (`degatsArme`) d'une unité en jeu. **Débarquement choisi** : « Débarquer <nom> » par passager, cases voisines en vert, puis « Débarquer aussi <nom> » ou « Terminer », un seul ordre. L'aperçu raster dessine port et radar, sa liste de bâtiments dérivant enfin de `CARACTERES_CAPTURABLES` ; la vignette HUD dessine l'aile delta.
6. **Les pages** : `/jeu` liste les parties libres (la démo, puis les scénarios `en_ligne` cités par aucune épreuve de `content/campagne.json`, avec la partie en cours du profil actif) et l'accueil y mène ; le banc passe au catalogue 6 (48 unités posées), la vitrine déduit ses versions du canon ; `pieces.ts` fait de `ailes + plateau` une **aile delta** sans dérive ni queue (`aileVolante`, 1 048 triangles).
8. **La mention de version** (demande du soir) : en bas à droite de l'écran-titre et en pied des réglages, la date et l'heure de la mise en ligne à Paris et le commit court — « 7 sept. 2026, 21:14 · 89d3355 ». Calculée **au build** dans `next.config.ts` (`env.ATLAS_VERSION_DATE`, `ATLAS_VERSION_COMMIT` : `SOURCE_COMMIT` de Coolify, sinon `.git/HEAD` lu sans binaire git ; `ARG SOURCE_COMMIT` dans le `Dockerfile`), inscrite dans les bundles, donc changée à chaque push déployé, **sans action GitHub** ni fichier généré. `src/app/version.ts` (pur, testé) la lit et la compose par `t()` ; vide, rien ne s'affiche. Le format a été tranché par deux personas à la demande du propriétaire (un joueur novateur, un ado) : date, heure, commit, pas de préfixe, gris à 11 px, heure de Paris fixe, écran-titre et réglages. Leur idée en plus, **non faite** : un point orange quand le build diffère de la dernière visite.
9. **Au passage** : un registre des écarts aux bornes de conception du §13.4 dans `tests/schemas/contenu.test.ts` (drone, missiles sol, chasseur, bombardier, cuirassé ; un écart neuf y échoue, un écart résorbé s'en retire) ; `scripts/simuler.ts` compte « sans vainqueur » comme le serveur (manque n° 7 clos) ; README, `doc/README`, `doc/11` et `PLAN` étape 11 datés ; 972 specs (24 unités, 576 kits, 246 bâtiments).

**À trancher ou non vérifié** : tout le visuel — l'opacité 0,45 contre le verre 0,58 et la jouée 0,6, le glyphe, le menu à vignettes, la mise en page de `/jeu`, l'aile delta, le port et le radar de l'aperçu ; la ceinture de mer, qui fait de toute carte à ports une île ; le rejet de la carte côtière générée ; une **mémoire de dernière position vue** dans l'état filtré, façon Advance Wars, qui permettrait la chasse au sous-marin et au furtif ; l'écart doc/moteur sur le débarqué (« peut agir » au doc, `agi` au moteur, daté au §2 de `doc/04`).

## Mise à jour — catalogue 4, arme secondaire, brouillard à l'écran, unités jouées en transparence (7 septembre 2026, nuit)

Six demandes du propriétaire, dont il a tranché les points ouverts ; trois agents, fichiers disjoints ; 876 tests, `typecheck`, `lint`, `generer-specs-assets --verifier` (681) et `verifier:campagne` (6/6, rejeu conforme) verts. **Rien regardé à l'écran.** La sixième demande — un moteur d'animations transitoires (capture, construction, combat, pouvoir) — est **reportée** à sa demande.

1. **Les unités qui ont joué ne noircissent plus** : `Materiaux.terni()` rend un clone transparent (`OPACITE_JOUEE` 0,6, `depthWrite` gardé comme le verre), la figurine cesse de porter ombre pendant l'état joué, couleurs inchangées ; le **tassement est supprimé** (`TASSEMENT`, `enfoncer`, `couleurTernie` de `modeles.ts` disparus) ; le cadenas et l'immobilité restent.
2. **Catalogue 4.** Trois des quatre unités « manquantes » existaient (`char_leger`, `char_lourd`, `antiair`, `transport`) : `premier_contact` n'a ni usine ni fonds, on n'y voit jamais le menu de production. Nouveau : **`char_moyen`** (10 000, mouvement 5, vision 2, 8 munitions, `chenilles/bloc/[tourelle,canon_long]/2`, homologué au catalogue 4) ; le **transport devient ravitailleur** (`['transport','ravitaillement']`, 2 places, `infanterie`/`meca`/`genie`, 5 000) — le trait était implémenté de bout en bout et porté par personne ; **`drone_filaire` supprimé du canon** (jamais joué ni posé ; ses 25 specs disparaissent, 25 de `char_moyen` arrivent, total inchangé). Toutes les missions sauf `demo` passent en `catalogueVersion: 4`. `scripts/simuler.ts` gagne `--catalogue N` (il jouait toujours le 2). Sur plaine.json × 20, le char moyen est acheté 99 fois et mange surtout le char lourd (59 → 27) ; le transport reste à 0, l'IA ne sait ni embarquer ni ravitailler (connu).
3. **Infanterie → char léger : 25 → 10** (`degats.json` et la ligne de l'infanterie). Conséquence mesurée : la mission 6 n'était plus gagnable par les trois joueurs simples du vérificateur ; un agent avait remplacé le `char_leger` de départ du joueur sur `carte_couleurs_alliees` par un `char_moyen`. **Refusé par le propriétaire** le 7 septembre : la carte est revenue au `char_leger`, et c'est l'IA qui a été améliorée (mise à jour suivante) — la mission 6 est gagnée par la pondérée en 9 journées, carte et scénario intacts.
4. **L'arme secondaire** (`doc/04` §5.3, §10 ter) : `UnitType.armeSecondaire?: CleUnite[] | null` — contre ces cibles, l'unité tire à la mitrailleuse : aucune munition consommée, tir et riposte permis à zéro. Chars → `infanterie, meca, genie` (0 contre l'hélico reste 0 : le validateur refuse une cible à 0, et une armée de chars sans anti-air doit perdre contre trois hélicos) ; `meca` et `helico` → les mêmes plus `helico` et `drone`. `tireSansMunitions(type, cible)` est exportée du moteur ; `prevoirDuel` applique la règle. La fiche marque ces cibles d'une pastille « ∞ » (`fiche.sans_munitions`). L'IA a appris la règle le 7 septembre (mise à jour suivante).
5. **Le carburant existait** (décrément, consommation par journée en l'air, `panne_seche`, ravitaillement gratuit sur bâtiment ami) mais n'était **pas visible**. Le panneau d'inspection porte `data-alerte="orange|rouge"` sur carburant (aérien : rouge à `≤ parTour`, orange sous `2·parTour` ; roulant : rouge à 0, orange sous 20 % du max) et munitions (rouge à 0, orange à 1), la panne sèche et le ravitaillement sont **annoncés** (`hud.panne_seche`, `hud.ravitaillement`), les unités à bord listées (`hud.embarquees`), et `panne_seche` devient un déclencheur de scène (`DECLENCHEURS_SCENE`, `doc/03`). Huit clés i18n neuves.
6. **Le brouillard à l'écran.** Le moteur était complet et les unités hors de vue déjà non dessinées ; **rien n'assombrissait les cases**, la nappe de la 2D n'avait pas été portée. Décision du propriétaire, qui l'emporte sur la proposition « assombri, pas noir » de `doc/04` §10 : d'abord « très sombre » (facteur 0,15), puis le 7 septembre **« noir noir, 100 % »** : `FACTEUR_BROUILLARD = 0`, teinte noire, drapeau à 0 aussi — un drapeau lisible dans le noir serait la seule chose visible et trahirait le propriétaire. `Plateau.majVisibles` écrit une `DataTexture` d'un octet par case, greffée dans les shaders du sol, du socle, des voies, des ponts et de l'eau (`FACTEUR_BROUILLARD` 0,15, teinte `#0a0e1a`, transition d'un demi-texel) ; bâtiments en doubles sombres, arbres/pierres/accessoires/rivage par couleur d'instance ou de sommet ; le drapeau reste hissé mais noir comme sa case. Rien par image : tout sur comparaison de l'ensemble. **Activé sur les missions 3 à 6** (les deux premières restent claires pour apprendre). Non vérifié à l'œil : le tri de la transparence 0,6 avec le verre 0,58.

**Constante à régler à l'œil** : `OPACITE_JOUEE` (`unites.ts`). `FACTEUR_BROUILLARD` et `FACTEUR_BROUILLARD_DRAPEAU` valent 0 par décision et restent là pour pouvoir revenir en arrière d'un chiffre.

## Mise à jour — catalogue 5 : neuf unités, le port, le paquet naval ouvert (7 septembre 2026)

Le propriétaire a demandé de compléter le catalogue, **le naval compris** — arbitrage n° 3 du `BRIEF.md` levé, étape 11 du `PLAN.md` entamée. Trois agents, fichiers disjoints. 950 tests, `typecheck`, `lint`, `verifier:campagne` 6/6 et les deux cartes neuves déclarées **valides** par la routine de contrôle.

1. **Neuf unités, catalogue 5** (`homologuee`, 7 septembre 2026). Air et missiles : `missiles_air` (12 000, portée 2-5, **ne frappe que le ciel**), `missiles_sol` (16 000, portée 3-6, sature sans percer un char lourd), `chasseur` (20 000, ne touche que ce qui vole), `bombardier` (18 000, écrase le sol, 0 en l'air), `transport_air` (6 000, deux fantassins par-dessus les montagnes). Naval : `barge` (6 000), `porte_avions` (20 000), `cuirasse` (19 000, artillerie de mer), `sous_marin` (12 000). Les six missions **restent en catalogue 4** : une simulation en 5 sur carte terrestre rend les mêmes chiffres qu'en 4, au chiffre près.
2. **Le terrain `port`** (caractère `O`), sixième capturable : défense 3, revenus 1 000, ravitaille, soigne, `mer: 1` dans ses coûts, et il produit les quatre navires. `mer` devient franchissable ; **plage, rivière et pont restent terrestres**, un navire ne passe donc que par la mer et le port. Une seule règle moteur ajoutée : `ravitailleCetteUnite` associe `mer` → port.
3. **Le trait `plongee`**, quatorzième : le sous-marin n'est repéré **qu'au contact** (`cacheeAuContact`). Qui le touche est sa colonne de dégâts, **pas une exception de combat** : cuirassé, sous-marin, hélicoptère, bombardier, porte-avions, et personne d'autre.
4. **Coques et ailes enfin portées.** Elles étaient écrites depuis le début et jamais vues : une boîte et un cône, deux plaques et un train. Réécrites **sans ajouter un nom au vocabulaire fermé** — `piecesBase` et `piecesCorps` sont devenues conscientes du couple (base, corps) : coque + `capsule` donne un submersible à kiosque, coque + `plateau` un pont plat que les modules tranchent en barge ou en porte-avions, coque + `bloc` un cuirassé. Les neuf unités tiennent entre 728 et 2 604 triangles, très loin de la borne de 6 000.
5. **Deux cartes de jeu libre**, hors campagne : `carte_archipel_des_deux_rades` (deux îles, une chaussée d'une case, la barge rend le goulot négociable) et `carte_bras_de_mer` (un continent où l'on gagne sans navire, mais où la mer est la route rapide). Contrôle **valide** pour les deux, 45 % et 53 % au camp qui commence.

**Quatre copies de listes supprimées** — la même faute quatre fois, et c'est la leçon : `CARACTERES_CAPTURABLES` (`schemas`), `CAPTURABLES` (`mapgen/grille.ts`), `RELIE_VOIE` et `BATIS` (`app/plateau-accueil.tsx`) recopiaient les bâtiments à la main. Toutes avaient déjà divergé au radar ou au port : le générateur ne comptait plus les ports dans la valeur d'une carte, l'aperçu texte n'avait pas leur légende, et l'accueil les dessinait en case nue sans raccorder leurs routes. Les quatre **dérivent** désormais de `TERRAINS_CAPTURABLES`. Au passage, `CLE_PRISE` (`render/jeu.ts`) n'annonçait ni le port ni la station radar : deux clés neuves.

**Le plafond des unités de départ passe de 40 à 60** (`validerMapDef`) : il visait une carte de mission, et la carte-catalogue du banc pose tout le catalogue dans les deux camps, soit 46 unités. L'agent du rendu a **refusé de contourner** et l'a signalé ; c'était le bon réflexe.

**Ce qui reste, et c'est le plus gros : l'IA ne sait rien de la mer.** Elle achète barges et transports d'assaut par son score de soutien, jamais un chasseur, un porte-avions ni un lance-missiles — les paliers au-dessus de 18 000 sont hors de sa portée d'achat, comme l'était déjà le lance-roquettes. Elle ne sait ni escorter, ni débarquer sur une côte adverse, ni chasser un sous-marin. Sur l'archipel, un camp gagne 90 % du temps, dont 18 parties sur 20 au chronomètre. **Trois écarts de conception** sont documentés dans `doc/04` §10 quater plutôt que cachés : le porte-avions frappe le ciel sans porter `anti_air`, le chasseur et le bombardier dépassent la borne de mouvement des unités chères, deux portées vont à six cases contre cinq. **Aucune de ces bornes n'est vérifiée par du code.** Enfin : **23 unités sur 24**, il ne reste qu'une place, et les 24 unités spéciales par nation de `doc/11` §10.3 n'y tiendront jamais.

**Rien ne mène aux deux cartes neuves** : le bouton « Jeu libre » de l'accueil pointe en dur sur `/jeu/demo` et il n'existe aucune liste. Elles se jouent à `/jeu/archipel_des_deux_rades` et `/jeu/bras_de_mer`. À trancher. **Tranché le soir même** : `/jeu` les liste, et l'accueil y mène.

## Mise à jour — le brouillard ne se laisse plus lire par les surbrillances (7 septembre 2026)

Trois remarques du propriétaire en jouant, et ce qu'elles valaient après vérification.

1. **« On voit au-delà du déplacement, la portée d'attaque laisse voir les textures » — vrai, et corrigé.** Les surbrillances n'étaient **jamais** bornées à la vision : `visibles` n'apparaissait ni dans `controleur.ts`, ni dans `objectifs.ts`, ni dans `surbrillances.ts`. Déplacement, enveloppe de tir et objectifs en or étaient donc peints sur des cases hors de vue, et comme un décalque épouse le relief, la bosse rouge dessinait la montagne sous le noir. Décision du propriétaire, parmi trois options : **garder les surbrillances, à plat et sans relief**. Chaque genre a une seconde nappe (`ALTITUDE_BROUILLARD`, `depthTest` éteint, `renderOrder` 4) pour les cases cachées ; le test de profondeur est éteint **exprès**, sinon la montagne noire découperait un trou en forme de montagne. `Rendu` : `CoucheSurbrillances.majVisibles(visibles)`, appelée par `majMonde`, ne rebâtit que si le partage vu/caché change.
2. **« On n'a pas de notion de vision avec les unités » — l'impression, pas le code.** Chaque type a sa portée (infanterie 2, char léger 3, char lourd 1, recon et drone 5), modulée par la nuit (−2), la pluie (−1), la brume (=1), la montagne (+2, +3 avec `vision_etendue`) et le brouillage (×0,1) ; forêts et montagnes cachent au-delà du contact. Deux causes à l'impression : les missions 1 et 2 n'ont pas de brouillard, et **chaque bâtiment possédé voit à 2 cases** (5 pour un radar), ce qui découvre une large part d'une carte 12 × 10 sans qu'aucune unité n'y soit pour rien. Le propriétaire a choisi de **garder les 2 cases**.
3. **« Le brouillard doit rester parfois toute la partie » — c'est déjà le cas**, et il reste actif la nuit et par temps de brume même quand le scénario ne le déclare pas (`brouillardActif`). Gardé tel quel : « la nuit rend aveugle, et la corporation peut tricher et le laisser en plein jour » — ce que `reglages.brouillard` permet déjà, missions 3 à 6.

4. **« Je peux voir ce que je ne devrais pas voir » — capture à l'appui, et c'était une erreur de méthode.** Le sol s'éteignait par le nuanceur, mais le décor par la **couleur** : matériau noirci pour les bâtiments, couleur d'instance noire pour arbres, pierres, accessoires et pavillons. Or **un matériau noir garde le reflet de l'environnement et l'éclat du soleil** : bâtiments, taches de sable et rochers restaient lisibles en gris à travers le noir. Tout le décor lit désormais le **même masque que le sol**, greffé sur ses matériaux (`grefferBrouillardSur`, `Plateau.uniformesBrouillard`), appliqué **après l'éclairage**. Deux pièges au passage : le sommet doit appliquer `instanceMatrix` avant `modelMatrix`, sinon toutes les instances d'un lot lisent la case de l'origine du lot ; et la clé de programme doit **composer** avec celle du matériau greffé. L'ancien mécanisme par la couleur est **retiré** — deux mécanismes pour une règle en font une règle fausse. `majProprietaires` ne garde de la vision que **la règle** : une unité cachée ne rend pas son bâtiment translucide.

5. **Les points de vie ne devancent plus le coup.** `EtatVisuel.pv` retient les points d'avant le geste ; `encaisser` les lit dans l'état précédent à son départ et les relâche à sa fin. L'étiquette d'une figurine tombait à l'instant de l'ordre, avant même que le tir parte.

Au passage, **un test instable réparé** : `tests/render/hud-html.test.ts` cadençait l'écran de combat sur `Date.now()` et `requestAnimationFrame`, passait seul et tombait quand toute la suite tournait. `monterScenes` et `monterHudHtml` prennent désormais une `HorlogeScenes` injectable, comme `boucle.ts` et `scene.ts` ; le test l'avance lui-même. Cinq passes d'affilée, vertes.

## Mise à jour — le moteur d'animations : la partition (7 septembre 2026, nuit)

La sixième demande, reportée deux fois, est faite : « un moteur, un cerveau d'animations transitoires — capture, construction, combat, pouvoir — et un écran de combat ». **La mise en scène est séparée de l'exécution**, et c'est tout l'intérêt : `render3d/animations.ts` lisait les événements du moteur et décidait seul des durées, de l'ordre et des positions ; le HUD ne savait rien de ce qui se jouait ; dix-neuf événements sur vingt-quatre n'avaient aucun geste. `doc/10` §7.6 est le document de référence.

1. **Le contrat, `src/render/partition.ts`** (pur, aucun `three`) : une **partition** est une liste de **gestes datés** — genre, qui, d'où, vers où, `debut` et `duree` en millisecondes, positions en **cases** — et sa durée totale. Dix-huit genres : `glisser, tirer, encaisser, sortir, hisser, remettre, batir, apparaitre, embarquer, debarquer, fusionner, ravitailler, reparer, repousser, pouvoir, duel, chiffre, cadrer`. `DUREES` porte les durées de référence, `MISE_EN_SCENE` les réglages qui n'en sont pas. **Le contrat ne fait que s'étendre** : on y ajoute, on ne renomme ni ne retire — deux exécutants le lisent sans se connaître.
2. **Le réalisateur, `ecrirePartition(evenements, avant, apres, options)`** (pur, testé) : le pouvoir passe en tête (le moteur le pousse en dernier) et ses constructions se posent l'une après l'autre à 120 ms d'écart ; une unité ne fait **qu'un geste à la fois** ; une unité agit **d'où elle est arrivée** ; une capture attend la palissade tombée ; les chiffres de dégâts sont en PV affichés, teintés `perte` ou `gain` selon le camp du joueur ; sous animations réduites, toutes les durées **et** les débuts valent 0, aucun geste n'est perdu.
3. **Deux exécutants.** Le HUD (`render/scenes-html.ts`, conteneur frère des onze emplacements) joue `chiffre`, `duel` et `pouvoir` : chiffres flottants ancrés par `versEcran` relu à chaque image, **écran de combat** semi-couvrant (vignettes 96 px, jauges de PV qui tombent au coup puis à la riposte), **splash de pouvoir** avec le buste du commandant (`buste` et `VISAGES` exportés de `dialogue-html.ts`). La peau 3D (`render3d/animations.ts`, devenu **interprète** : `gesteVersAnimation`, `animationsDePartition`) joue tout le reste, dont les gestes qui n'existaient pas : `batir`, `apparaitre`, `embarquer`, `debarquer`, `fusionner`, `ravitailler`, `reparer`, `repousser`, `pouvoir`, `cadrer`.
4. **`Rendu.jouer(partition)` et `Rendu.couper()`.** La coupure était écrite dans `doc/10` §7.3 depuis toujours et **branchée nulle part** : `Boucle.viderFile(true)` existait sans appelant. Un clic ou une touche pendant une salve coupe tout, chaque geste pose son état final exact, et le clic ne vaut pas ordre. `Rendu.animer` survit comme repli (`partitionProvisoire`).
5. **Le pool d'effets, `render3d/effets.ts`** (neuf) : six genres (`eclair, etincelle, halo, poussiere, anneau, caisse`), **capacité 64** dont un quart réservé aux effets couchés au sol, recyclage du plus vieux, un matériau par genre, aucune allocation par image, textures par instance de module (le `textureEclair` immortel a disparu), `dispose()` au démontage — le groupe `effets` n'était pas libéré. `salir(ombre)` distingue ce qui déplace un porteur d'ombre de ce qui n'est que sprite et lumière : une étincelle ne coûte plus une carte d'ombre.
6. **Décisions du propriétaire** : la caméra recadre pendant le tour de l'IA **seulement si** l'unité qui agit est hors champ (`cadrerCase`, marge de 16 %), jamais pour ses propres actions, et **rend au joueur la vue qu'il avait** quand la main lui revient (`Vue3d.retenirVue`/`revenirVue`, `Rendu.retenirVue`/`revenirVue`, appelées aux deux bouts de `tourAdversaire`) — sauf s'il a lui-même bougé la caméra entre-temps, auquel cas la mémoire est oubliée : on ne ramène personne là où il a choisi de ne plus être ; combat avec chiffres flottants **et** étincelles ; pouvoir avec splash complet. L'**écran de combat** a son réglage, `Preferences.ecranCombat`, **vrai par défaut**, dans `/reglages`.

**Non vérifié à l'écran** (consigne) : la lisibilité des étincelles, la force de l'éclat de pouvoir (`ECLAT_MAX`), la densité des anneaux, la mise en page de l'écran de combat. **Non fait** : en repli `animer` (peau sans `jouer`), un clic coupe le HUD sans faire sauter la 3D — sans objet puisque la peau implémente `jouer`. 928 tests, `typecheck`, `lint`, `verifier:campagne` 6/6, specs 681, chaînes à blanc : verts.

## Mise à jour — quatre retours de jeu du propriétaire (7 septembre 2026, suite)

1. **Le tir partait de la case de départ.** `render3d/animations.ts` lisait la position de l'attaquant dans l'état `avant` l'ordre, où l'unité n'avait pas bougé : cap et éclair de bouche partaient de l'origine pendant que la figurine était déjà à l'arrivée. La salve tient une table `positions` (id → case) remplie par chaque `deplacement` ; le tir part de l'arrivée, la **riposte vise l'arrivée** et a son propre éclair. Testé.
2. **« Manche gagnée » s'affichait deux fois.** `controleur.jouer` rafraîchissait la vue dès l'état terminé, avant que `dialogueVictoire` soit enfilé à la fin de l'animation : écran de fin, puis dialogue qui le cache, puis écran de fin. Règle : `retenirFin()` retient la fin **synchroniquement** dès que la partie se termine ; `dialogueFin()` (`render/dialogues.ts`, pure) enfile le dialogue du scénario ou un **repli** dit par le commandant du joueur (`dialogue.victoire_defaut` / `dialogue.defaite_defaut`) ; `ecranFin` ne se rend que terminée **et** sans scène ouverte ni fin en attente (`VueJeu.finEnAttente`). Sans dialogues (`/reglages`), l'écran vient tout de suite. Il y a donc **toujours** un dialogue de fin, avant l'écran.
3. **Un char à sec tire sur un char.** Correction de la règle du 7 septembre : à zéro munition, une unité qui a une arme secondaire vise **toute** cible à dégâts > 0 — dégâts pleins sur ses cibles listées, `UnitType.degatsSecondaire` comme base sinon (15 chars, 10 méca et hélico ; validateur 1-30, exige `armeSecondaire` et des munitions), sans munition consommée, riposte comprise. `degatsArme(cat, att, cible)` (moteur) est **la** source : `calculerDegats`, la riposte, `prevoirDuel` et l'IA la lisent. `doc/04` §5.3.
4. **Un transport s'achète seul, rarement.** Le zéro dur de `valeurSoutien` sous trois unités armées est retiré : sans armée, un soutien vaut la moitié de sa valeur si aucun soutien n'est en jeu **et** qu'un besoin réel existe (un capteur à plus de deux tours de son objectif, ou une unité aérienne loin de tout ravitaillement), et la règle des fonds cède quand ce besoin est le seul et qu'aucun adversaire n'est à huit cases. Mesuré : 11 transports sur 20 parties de plaine, inchangé. L'IA lit `degatsArme` : un char à sec compte comme une menace réduite contre un char, pleine contre l'infanterie, et attaque un char à sec quand rien de mieux n'existe.

902 tests, `typecheck`, `lint`, `verifier:campagne` 6/6, specs à jour. Rien regardé à l'écran, rien commité.

## Mise à jour — le cerveau logistique de l'IA (7 septembre 2026)

« L'IA ne sait ni embarquer ni ravitailler, et ne tire pas à l'arme secondaire une fois à sec : apprends-lui. » Fait dans `src/ai/logistique.ts` (neuf, pur) et les trois stratégies, sans un nom d'unité, sans toucher au moteur ni au contenu ; `doc/04` §12.7 et `doc/02` §3.2 portent la mention datée.

1. **Arme secondaire** : `peutTirerSur` (munitions, tir illimité, ou `tireSansMunitions` du moteur) remplace `aDesMunitions` ; la menace (`frappeurs`, `menaceSur`) ne compte un adversaire à sec que contre ses cibles secondaires ; la **dernière munition a un prix** (`poids.munitions × coût/1000 / restantes`), soustrait aux tirs à l'arme principale — assez pour trancher les cas serrés, pas plus.
2. **Carburant et ravitaillement** : `autonomieSecurite` fait rentrer une unité aérienne vers l'aéroport ou la ville ami avant la panne sèche ; une unité à court (≤ 1 munition hors secondaires, ou moins de deux tours de carburant) progresse vers une source (bâtiment ami du bon domaine, ou voisine d'un ravitailleur allié) ; le **ravitailleur** émet `ravitailler` vers tout voisin qui manque, marche vers ses clients, et sa menace compte ce qu'il porte.
3. **Embarquer / débarquer** : `embarquementsUtiles` compare les tours à pied et embarqué (attente + trajet + un tour) et n'embarque qu'à un tour gagné ; le transport débarque sur une voisine libre franchissable quand le passager y capture, atteint son objectif au tour suivant, ou quand le transport est menacé sans pouvoir fuir. Les pièces de soutien jouent **en dernier** : embarqué puis transporté le même tour.
4. **Achats** : `valeurSoutien` (0,12) donne un prix au transport et au ravitailleur — jamais sous trois unités armées, jamais si un soutien couvre déjà six armées, jamais si l'achat empêche une unité armée au tour suivant — et une valeur d'information au drone sous brouillard et au brouilleur face à un drone adverse. Mesuré : à 0,3 l'IA achetait 102 transports et la plaine se décidait en 24 journées médianes ; à 0,12, **11 transports** sur 20 parties.
5. **Deux défauts préexistants trouvés par les missions** : un non-capteur se garait sur la ville qu'une infanterie venait prendre et les deux IA se **figeaient** quatorze journées face à face (mission 2 ; −`capture`/2 quand un capteur ami est à ≤ 3 cases) ; et l'artillerie proposait un tir sur une case invisible, dont le refus `cible_invisible` **fermait le tour** de l'IA toutes les nuits. Le génie va aussi vers les désaffectés.

**Chiffres** (20 parties, graine 1, catalogue 4) : plaine 45/55 → 35/65 pondérée/agressive, `limite_journees` **16 → 5**, médiane 41 → 28 journées (le manque n° 6, « l'IA va aux points », recule nettement) ; relief 90/10 → 80/20, inchangé aux points. `verifier:campagne` 6/6, rejeu conforme, **mission 6 gagnée par la pondérée en 9 journées avec le `char_leger` d'origine** — l'heuristique la perd encore, ses capteurs n'attaquent jamais un occupant. **Non fait** : `fusionner`, l'usage tactique du drone et du brouilleur (achetés avec mesure, traités comme avant), l'IA lit encore tout `etat.unites` sous brouillard (préexistant).

## Mise à jour — l'image de base, le bleu autour, et la fin du survol (6 septembre 2026, nuit, second lot)

Le propriétaire sentait encore un délai après le premier lot, et demandait si « le bleu autour » pouvait disparaître. Trois agents, fichiers disjoints, même arbre de travail ; ce qui suit est vérifié par 849 tests, `typecheck` et `lint`, et **rien n'a été regardé à l'écran**.

1. **La chaîne GTAO s'allumait sur ce M1 et divisait la cadence par deux.** Le commit `4754bb2` avait monté le seuil `auto` de 8 à 20 ms « sur la mesure » ; or l'image nue fait 12 à 18 ms sur cette machine et la chaîne coûte 2,3×, donc 25 à 36 images par seconde en jeu — c'est très probablement le délai ressenti. `render/qualite.ts` : le seuil vaut désormais **7 ms** (`BUDGET_MS_IMAGE` 16,7 / `FACTEUR_COMPOSEUR` 2,3), et une **rétroaction** l'éteint pour la session dès que la médiane des trente derniers intervalles entre images consécutives dépasse 24 ms (`msCadence`, `cadenceInsuffisante`, `decisionComposeur(…, cadenceRefusee)`) — c'est la seule mesure qui compte le GPU sans bloquer. `haute` reste inconditionnel.
2. **La carte d'ombre n'est plus recalculée à chaque image.** `scene.ts` `dessiner(camera, { ombre, continu })` ; `index.ts` ne demande une ombre que sur un nouvel état (`afficher` avec `e !== etat`), une unité posée/apparue/disparue (`unites.maj` rend un booléen), une animation de la file y compris son dernier pas, une mutation de terrain, ou un cadre d'ombre qui change d'objet (`cadrerOmbre` rend le même objet tant que caméra et soleil n'ont pas bougé). Respiration, drapeaux, particules, anneau et eau **ne valent pas une ombre** ; une unité qui glisse en porte une qui suit. Les trois `appliquerAmbiance` ne sont appelés que si `eclairage.courant` ou la saison ont changé d'objet (hors transition, `courant` est stable ; c'est le contrat).
3. **Le bleu autour était le plan d'eau** (`terrain.ts`) : 1,6 fois la carte plus 30 unités, en matériau standard éclairé, ombré, à normal map animée — du remplissage de pixels sur presque tout l'écran. Il déborde désormais d'**une case** (`DEBORD_EAU`), et autour on voit la couleur du ciel, gratuite ; le socle existant ferme déjà le bord. Le brouillard de scène prend **exactement** la couleur du fond, sans quoi le bord du plateau fondrait vers une autre teinte par temps de brouillard. Trouvés au passage et corrigés : eau, socle et grille n'étaient pas redimensionnés au changement de carte (l'atelier), les fonds lus par le shader (`tFonds`) étaient figés après une marée, et l'écume de marée était **annulée chaque image** par `appliquerAmbiance` — elle n'avait jamais été rendue. **Deux options laissées au propriétaire** : un fond neutre au lieu du ciel (par exemple `melanger(p.ciel, '#1e232b', 0.6)` dans `eclairage.ts`), ou un débord nul, qui exigerait de raccourcir le socle à −0,6 et de le passer en matériau basique.
4. **Ce qui restait sur le chemin du survol.** `signatureTerrain` ne joint plus la grille qu'une fois par tableau (`WeakMap` sur la grille, jamais sur l'état : le moteur **écrit en place** sur son état de travail après le clone de `appliquer` — journée, données de mécanique, terrains posés, propriétaires —, un cache par identité d'état mentirait) ; `decor.majProprietaires` sort sur une clé d'aspect (bâtiments, occupants visibles, points de capture, lueurs) au lieu de renvoyer trois lots d'instances au GPU par survol ; `unites.maj` compare une `PoseUnite` par unité et ne repose que ce qui a changé, `majRelief()`/`suivreSol(ms)` reposent tout pendant une mutation ; la toile des drapeaux bat à 30 Hz au plus ; `decor`/`paysage.appliquerAmbiance` sortent sur identité.

5. **Le réglage `haute` est retiré** (demande du propriétaire, dans la foulée : « ça lague trop »). `QualiteRendu` vaut `auto | basse` ; `normaliserQualite` ramène un `haute` enregistré à `auto` ; la chaîne `reglages.qualite_haute` a disparu de `content/i18n/interface.fr.json` et la note du réglage est réécrite. Son relevé `__atlas.mesurer()` montrait `composeur: true` avec `msCalibration: null`, la signature exacte de `haute` : le réglage forçait ce que la mesure aurait refusé.

**Ce qui tourne encore à 60 Hz en partie** : la respiration des figurines et les rotors (deux sinus par unité, mais ce sont eux qui gardent la boucle éveillée), l'eau, la toile, les particules. **Non fait** : `casesObjectifs` et `visibles()` reconstruits par vue (coût faible), `msCadence` absent de `MesuresRendu`, le gain sur cette machine non chiffré.

## Mise à jour — le survol ne traîne plus (6 septembre 2026, nuit)

Le propriétaire sentait un délai au survol de la souris sur `/jeu/premier_contact`. Le chemin d'un `pointermove` a été tracé, et il avait deux étages qui suffisaient chacun : **à chaque événement**, un lancer de rayon en force brute sur les ~7 000 triangles du sol, sans coalescence ; **à chaque changement de case**, `rafraichir()` refaisait tout sans rien comparer — le HUD remplaçait ses onze panneaux par `innerHTML`, la couche de surbrillances détruisait et rebâtissait ses sept géométries, le contrôleur rejouait le Dijkstra de `portee()` pour le chemin et recalculait trois fois sa vue (donc trois fois l'enveloppe de tir), parce que `Controleur.vue` est un getter lu par trois fonctions. Quatre corrections, toutes testées :

1. **Le HUD vit dans onze emplacements** (`EMPLACEMENTS`, `poserEmplacements` — pure, exportée) en `display:contents`, chacun réécrit seulement si son HTML a changé ; les vignettes portent un identifiant **par emplacement** (`vg_inspection_0`), sans quoi une vignette qui disparaît d'un panneau renumérotait celles du suivant. `replacerFocus` ne court que si la modale de production a été réécrite. `tests/render/hud-html.test.ts` compte les écritures d'`innerHTML` sur un document factice : une vue identique n'écrit rien, survoler une unité ne réécrit que l'inspection.
2. **Les surbrillances ont une clé par maille** (`render3d/surbrillances.ts`) : au survol, seules la flèche, son liseré et le curseur se rebâtissent. `invalider()` rebâtit tout sur la dernière vue, et `dessiner` l'appelle pendant une mutation de terrain, de sorte que les décalques suivent la marée au lieu d'attendre la vue suivante.
3. **Le contrôleur calcule une fois** : `deplacementDe` garde la `Portee` du moteur avec les atteignables (le chemin la relit), `porteeAttaque` est mémoïsée sur l'identité de l'ensemble atteignable, et `jeu.ts` lit `controleur.vue` **une fois par `rafraichir`** (`vueCourante`). Le test compte les lectures de `type.carburant`, que seul `portee()` fait pendant un survol : dix survols, zéro nouveau calcul.
4. **Le survol est coalescé sur l'image** (`gestes.ts`, `planifierImage` injectable ; sans `requestAnimationFrame`, les tests le traitent sur-le-champ) et `caseSous` n'alloue plus rien.

**Mesuré** par les compteurs `Performance` de Chrome (CDP) sur le vrai GPU, en soustrayant une fenêtre témoin sans souris, deux passes chacune, sur `/jeu/demo` en phase de sélection avec 120 mouvements sur neuf cases : le surcoût processeur du survol passe d'environ 300 ms par seconde de souris à 140, la mise en page et le style de 300 ms à 22, et les mutations DOM du HUD de 120 à 26 pour le même trajet. **Pas fait, consigné** : `shadowMap.needsUpdate` inconditionnel à chaque image (`scene.ts`), la boucle qui ne dort jamais en partie (drapeaux, respiration des figurines), `decor.majProprietaires` qui rebalaye les bâtiments à chaque vue.

**Sur la fumée Playwright, une leçon** : le 6 septembre au soir, avec la charge moyenne à 14 sur 8 cœurs (un Chrome qui joue au jeu, Docker, VS Code), la fumée SwiftShader dépassait ses 180 s **sur `main` comme sur la branche**, au même endroit — un échec de machine, pas de code. Comparer deux révisions se fait avec un worktree détaché **sous `.claude/worktrees/`** (Turbopack refuse un `node_modules` symlinké hors de la racine du dépôt) et un `NEXT_DIST_DIR` à part ; et une copie du spec avec `--use-angle=metal` à la place de SwiftShader passe la fumée entière en 29 s sur ce M1, ce qui en fait le bon outil quand la machine n'est pas calme.

## Mise à jour — le plan de réalisme exécuté : lots A, B0 et C, par agents (6 septembre 2026, soir)

Le propriétaire a demandé d'exécuter `doc/16-realisme.md` « par des agents spécialisés » et de pousser sur `main`. Trois agents ont travaillé en parallèle dans des worktrees git (`.claude/worktrees/`, ignoré par git et par ESLint), un quatrième a relu la fusion sans rien modifier, un cinquième a mesuré ; ce qui suit est ce qui en est sorti, et ce qui n'a pas pu l'être. **Aucun `.glb` n'existe toujours** : tout ce qui suit prépare le premier fichier, il ne le remplace pas.

1. **Lot A, en entier.** `render3d/environnement.ts` cuit une `RoomEnvironment` par `PMREMGenerator` une fois au montage ; `ParametresAmbiance.environnement { intensite, teinte }` la module (jour ≈ 0,3, nuit 0,08, refroidie l'hiver, voilée par la météo — la teinte est calculée et testée mais r170 n'offre aucun moyen de l'appliquer sans recuire la pièce : elle attend). `render3d/ombres.ts` (pur, testé contre la vraie `DirectionalLightShadow`) resserre la caméra d'ombre sur le **champ visible** au lieu de la carte entière, 2048² sur ordinateur et 1024² sous `pointer: coarse`, biais en texels ; `PCFSoftShadowMap` est gardé (son `radius` est ignoré, et le PCF plus VSM bandent ou fuient) ; la carte d'ombre n'est calculée qu'**une fois par image** (`shadowMap.autoUpdate = false`), sans quoi la passe des normales du GTAO la redessinait. `render3d/postraitement.ts` : `RenderPass` en demi-flottants multi-échantillons → `GTAOPass` → `OutputPass` → vignette 12 % / grain 2 % / saturation +8 %, chargé par `import()` à l'activation (l'accueil ne l'embarque pas). **Le réglage « Qualité d'affichage »** (`Preferences.qualite`, `render/qualite.ts`, `/reglages`) vaut `auto | haute | basse` et pilote une seule chose, réellement : `basse` jamais, `haute` toujours, `auto` mesure la **médiane de dix images** après la première et allume sous 20 ms — 8 au premier réglage ; la mesure A6 donne 12 à 18 ms l'image nue sur ce M1 et 25 à 36 images par seconde avec la chaîne — ; « animations réduites » éteint tout. Le calcul est pur (`decisionComposeur`, `composeurPossible` pour l'extension `EXT_color_buffer_float`) et testé. **A5** : les sept rôles de `Materiaux` sont des matières — tôle peinte à émission presque nulle, acier, **verre translucide** qui ne porte plus d'ombre, caoutchouc, peau mate —, la pluie les mouille par jeu de matériaux (`CalqueUnites.appliquerAmbiance`), et les toits reçoivent tuiles, ardoises ou tôle en `DataTexture` (`jeuToit`, `cartographierToit`). **A6** : `Rendu.mesurer()` — `window.__atlas.mesurer()` en jeu, `__atlasBanc.mesurer()` et `qualite(v)` sur le banc, section « Rendu » du dock — et la campagne de mesure est consignée dans `doc/10` §9.2 avec sa méthode.

2. **B0, les cinq préalables du chargeur**, dans un module neuf `render3d/modeles.ts` : une I/O mince (`GLTFLoader`, `_lod0/_lod1/_lod2` seuls — le nom sans suffixe n'est plus essayé —, et **un inventaire** lu une fois par page sur `/api/modeles`, qui liste `public/assets/modeles/` à la demande : le chargeur ne demande que les fichiers listés, donc plus aucune sonde à vide — le propriétaire voyait quarante 404 par page ; sans la route, il retombe sur le sondage, un 404 mémorisé par nom) et une **conformation pure** testée avec des groupes construits en mémoire. La rotation à l'arrivée est de **+π/2 autour de Y** — le plan écrivait −π/2, un test sur un point a tranché ; le gabarit `a/b/c` s'applique sur une enveloppe **extérieure** à la rotation (three compose T·R·S : sur le même groupe, la longueur tombait sur la largeur, ce que la relecture a trouvé) ; un `THREE.LOD` aux paliers de zoom ; le masque d'équipe se mélange dans le shader par `onBeforeCompile` (plein sur une base, liseré seul sur un kit ; la texture est retrouvée par une `WeakMap`, pas `userData`, que `clone()` sérialise). `scripts/controler-asset.ts` (`npm run controler:asset -- --spec … --glb …`) imprime le verdict de `validerGlb`, code 1 sur refus. La vitrine dit **modèle livré / placeholder**, laisse forcer un LOD et joue les clips d'un fichier.

3. **Lot C, la partie code.** `EtatVisuel.clip` est le clip logique que les animations posent (`deplacement` pendant le glissement, `tir`, `touche`, `hors_jeu`, `capture`, `repos` à la fin ou à l'interruption) ; un modèle livré le joue dans un `AnimationMixer` par unité avec un fondu de 150 ms, `repos` et `deplacement` bouclent, un clip absent retombe sur `repos`. Les gestes d'une même unité sont **séquencés** dans une salve (`retard` d'`ajouter`, enfin utilisé) : la figurine ne tire plus en marchant, mais une salve « avancer, tirer, encaisser » dure la marche plus 260 ms de plus qu'avant. Tranché (§4.4 du plan) : **un seul mécanisme**, le mixer joue un squelette comme une transformation de nœuds nommés. Les six clips eux-mêmes sont une production Blender et **n'existent pas**.

4. **Ce qui n'a pas été fait, et pourquoi** : B1 (le pilote du char léger) demande un générateur image-vers-3D ou un modeleur, donc un compte, un outil, du temps humain — rien de cela ne s'exécute par du code ; B2, B3 et les clips en dépendent. `public/assets/modeles/` n'existe pas.

5. **Deux fautes trouvées par la fumée Playwright après la fusion, à ne pas réintroduire.** La calibration `auto` mesurait avec `gl.finish()`, que Chrome traite comme un `flush()` : sous SwiftShader, une image d'**une seconde se mesurait à zéro** et la chaîne s'allumait sur l'appareil le plus lent qui soit. La barrière synchrone est `readPixels` d'un pixel (`scene.ts`, `attendreDessin`). Et le HUD **remplaçait tout son DOM toutes les 1,5 s** tant qu'une unité était sous le curseur, parce que le rappel du sursis d'inspection se réarmait à chaque rendu (`hud-html.ts`) : chaque clic Playwright attendait vingt secondes qu'un bouton reste en place. Le rappel ne s'arme que lorsque le sursis est la seule raison de retenir l'unité. Mesures sous SwiftShader à 1280 × 800 sur la démo : 811 ms l'image nue avec l'environnement, 524 sans, 808 avec des ombres 1024² ; 1 000 ms l'image en animation contre 909 avant le lot A, et un tour d'IA de 22 s dans les deux cas. **La fumée dure 2,3 minutes sur un serveur calme** ; sous contention (un `npm test` ou un `next build` en parallèle) elle dépasse ses 180 s. La lancer sur une machine calme, ou sur un serveur dédié : `NEXT_DIST_DIR=.next-build npx next dev --turbopack -p 3402` puis `E2E_BASE_URL=http://localhost:3402 npm run test:e2e` — `next dev` ajoute son dossier de types à `tsconfig.json`, à ne pas commiter.

6. **Ce qui reste fragile ou ouvert** : `msParImage` est le temps d'envoi côté JavaScript, pas le coût de l'image — seule `msCalibration` attend le processeur graphique ; la démo 12 × 10 fait **336 appels de dessin** contre 27 visés (chaque placeholder est sept mailles et un socle) et le remède est l'instanciation par type de `doc/10` §9.1, pas faite ; les durées de clips de la spec (700 / 500 / 900 ms) ne sont pas celles des gestes du rendu (260 / 300 / 420), le rendu ajuste le clip au geste ; le shader du masque n'a jamais été compilé par un vrai WebGL ; rien de tout cela n'a été regardé à l'écran, conformément à la consigne.

## Mise à jour — direction artistique tranchée et plan de réalisme (6 septembre 2026, après-midi)

**« Plutôt réaliste, mais un ton léger et cartoon »**, tranché par Thief et écrit dans `BRIEF.md` (Direction artistique) et `doc/10` §2. Le plan pour y arriver est **`doc/16-realisme.md`**, enregistré dans `doc/README.md` et renvoyé depuis `PLAN.md` étape 4 : lot A le moteur (environnement PMREM, ombres, occlusion GTAO, vignettage, mesure), lot B les vrais modèles (cinq préalables du chargeur — il pose aujourd'hui un GLB **sans rotation** alors que la spec impose l'avant en `+Z` et le placeholder regarde `+X`, sans gabarit, sans LOD, sans mixer —, un pilote sur le char léger, puis la priorité 1 famille par famille), lot C l'animation squelettique. Rien de ce plan n'était commencé à l'heure où ceci s'écrivait ; **les lots A, B0 et la partie code du lot C ont été exécutés le soir même** (mise à jour ci-dessus). Le constat de départ reste vrai : **aucun modèle 3D n'existe**, `public/` est vide, et la boucle de `doc/11` §2 n'a jamais tourné.

## Mise à jour — des figurines à visage, et une caméra qui tourne (6 septembre 2026)

Deux retours du propriétaire : « les modèles 3D ne sont pas très réalistes, commençons par les unités à pied, et on veut voir les visages » ; « je ne peux que zoomer, je voudrais tourner ».

1. **La troupe à pied est redessinée** (`src/render3d/pieces.ts`, section « La troupe à pied » ; `doc/10` §7.4). Corps à cinq têtes et demie, membres en deux segments placés par la géométrie (`brasVers`, `jambe`, `segment`, `orientee` : de quoi mettre une pièce entre deux points, ce qui manquait), mains et visage dans un **septième rôle `peau`**, casque reculé pour que le visage dépasse devant lui vu d'en haut, équipement par métier, trois allures par escouade. Aucun nom d'unité : ce sont toujours les modules qui décident. **Le coût est tenu** : 5 760 triangles l'infanterie contre 5 532 avant, sous la borne de 6 000 du test, parce que `geometriePiece` donne moins de méridiens aux petites pièces et une boîte nue aux pièces minces — le char léger y a gagné un tiers. Le test « six matériaux au maximum » passe à sept. Le principe demandé — un squelette qui ne change que de couleur en repli, puis un modèle par nation — est exactement la doctrine déjà écrite (`doc/10` §7.1, §7.2 ; `doc/11` §5.2 bis) : `principal`/`sombre`/`clair` prennent la palette, et le kit national remplace le tout par un fichier.
2. **La rotation existait et n'était offerte à personne.** Q et E tournaient la caméra par quarts de tour, en 3D seulement, sans que rien ne le dise. `Rendu.tourner(sens)` rejoint `zoomer` dans l'interface ; le HUD pose deux boutons de part et d'autre du zoom (`hud.tourner_gauche`, `hud.tourner_droite`), le banc de l'atelier aussi, et `toucheDe` nomme Q et E `tourner_gauche`/`tourner_droite` — ils ne doublent plus « gauche » et « fin de tour », qui gardent A/flèches et T. Le doc `10` §3.2 est tenu : quatre orientations, jamais un continuum, et le tangage reste fixe.

3. **La vitrine des unités** (`/atelier/unites`, `src/app/atelier/unites/`), demandée dans la foulée : « un truc plus rapide pour tester chaque unité ». Une unité à la fois, sous **six angles** sur un seul canevas — face, profil gauche, profil droit, dos, dessus en orthographique, et l'angle du jeu à 68° en perspective —, avec le choix de l'unité, de la nation (ou aucune : palette du camp), du camp et de la version du catalogue. Pas de terrain ni de boucle : trois lumières de studio, un disque d'herbe pour l'ombre, un rendu à la demande. Un `.glb` livré y remplacerait le placeholder comme en jeu (`chargerModele`). `Materiaux` est exporté de `unites.ts` pour elle. Hors production, `window.__atlasVitrine.choisir(unite, pays, camp)` et `pret()` la pilotent : **treize secondes** la planche sous SwiftShader, contre trois minutes une photo du banc — c'est là qu'on juge une figurine désormais, le banc restant l'endroit où l'on juge un plateau.

**Pour photographier une figurine de près sur le banc** : il ne zoome pas plus près que le premier palier (5 unités), ce qui laisse une figurine à soixante pixels. Le gros plan se fait par un rendu à l'échelle 3 puis un recadrage au centre (`sips`), depuis un pilotage Playwright sur `window.__atlasBanc`, qui expose désormais `tourner`. Sous SwiftShader, le banc met près de deux minutes à être prêt : tout `waitForFunction` doit avoir un délai de cinq minutes, le défaut de trente secondes échoue.

## Mise à jour — l’équipe d’Atlas et les codes de camp (6 septembre 2026)

Une décision de lore, une conséquence dans le schéma. Le nom « la Cinquième Manche » a été rediscuté et **conservé**.

1. **La faction a une équipe sur le terrain.** `01-bible.md` §3.4 décrit désormais **la Sélection Méridienne**, dite « les Gris » : l’équipe d’exhibition et d’essai d’Atlas, financée par le Consortium Méridien, dirigée par Hadran Ost, en gris avec le badge orange du matériel à l’essai. Adversaire anodin de Dépêche à l’acte I, propriétaire du dépôt où l’on retrouve le matériel non homologué à l’acte II, **la faction sur le terrain** à l’acte III. C’est l’équivalent de Black Hole, sans rien rattacher à un pays réel : ce n’est **pas** une `Country`, et les règles dures du §3.4 tiennent. `08-narration-choix.md` §6 et le glossaire français ont le renvoi.
2. **Les codes de camp acceptent trois lettres.** `REGEX_CODE_PAYS` et la portée `pays.` de `REGEX_FLAG` passent de `[a-z]{2}` à `[a-z]{2,3}`. Les 24 nations **gardent** leur code ISO alpha-2 ; les trois lettres sont réservées aux camps sans drapeau, et `atl` est le premier — il sert aussi de code de terrain à Port-Méridien, qui n’en avait pas. Aucun contenu `atl` n’existe encore : ni style, ni commandant, ni scénario.

## Mise à jour — fusion des deux chantiers du 6 septembre (6 septembre 2026)

Deux branches avaient chacune écrit une **fiche d'unité dans le menu de production** sans se voir. `main` portait `src/render/fiche-unite.ts` (pur, testé : dégâts lus dans les deux sens, terrains, météo, avertissement du tir indirect) déplié en ligne dans la liste, plus la fiche sous le curseur et le panneau d'inspection retenu. La branche du tutoriel portait un menu **en deux colonnes** (liste à gauche, fiche à droite, clavier, première unité abordable mise en avant) avec un bilan « bonne contre / faible contre » calculé dans `hud-html.ts`. La fusion garde la **mise en page** de la seconde et le **contenu** de la première : `ficheProduction` pose l'en-tête, la grille de chiffres et les traits, puis `blocFiche(v, cle, false)` — le même bloc que sous le curseur, sans sa ligne de chiffres que la grille dit déjà. `bilanDegats` a disparu, `premiereAbordable` reste. Le conteneur de la colonne s'appelle `.panneau-fiche` : il partageait la classe `.fiche` avec le contenu, et deux jeux de règles CSS se marchaient dessus. Dans `attract.tsx`, c'est la version du tutoriel qui a été prise — cadrage de l'île après le premier `afficher()`, avec la caméra 3D refaite le même jour —, et les textes des traits sont ceux du catalogue 3.

`main` avait aussi reçu, pendant la fusion, « drapeaux, villes entières, fiche façon Advance Wars et décor qui suit la grille ». La fiche façon Advance Wars (`stat()`, glyphes, figurines) est gardée telle quelle. Pour le décor, c'est la version du tutoriel qui reste — elle portait déjà les pavillons sur tout bâtiment, le fanion de capture et la transparence, avec ses prises `drapeau()` et `chantier()` —, et **`majGrille` y a été porté** : arbres, rochers, mâts et paysage sont ressemés et rebâtis à la taille du nouveau semis, les bâtiments au prochain `majProprietaires`. Le banc garde ses gestes `capture`, `capture_en_cours`, `remise_en_service` et `fin_de_tour` au lieu de la capture en deux appuis de `main`.

## Mise à jour — un vrai tutoriel, des bâtiments désaffectés, des drones (6 septembre 2026)

Le propriétaire a joué les quatre entraînements et a dit deux choses : « les cartes sont similaires, c’est moche » et « aller sur des cases pour les allumer, c’est nul ». Les deux étaient vraies — les cartes 1 et 2 étaient identiques au caractère près, 3 et 4 quasiment, tout en plaine vide — et la question qu’il a posée (« demande à trois ados et à un joueur d’Advance Wars de trente-cinq ans ») a une réponse connue : un tutoriel d’Advance Wars apprend à se battre, puis à prendre des villes, puis à recruter, puis à prendre le QG. `doc/15-premiers-matchs.md` décrit les six missions ; ce qu’il faut retenir ici, c’est ce qui a bougé sous le contenu.

1. **Le QG se prend en quarante points**, quatre tours d’infanterie intacte (`seuilCapture`, `src/engine/regles/capture.ts`). `SEUIL_CAPTURE` reste l’unité de compte à 20 ; le QG et un bâtiment désaffecté valent le double. **L’IA ne bouche plus son QG** d’une recrue chaque journée (`RAYON_MENACE_QG`, `src/ai/strategies/ponderee.ts`) : elle ne produit dessus que sans autre producteur libre ou avec un capteur adverse à huit cases. Sans cette règle, `capture_qg` n’avait aucun sens contre l’IA — la case était occupée à chaque tour du joueur. Mesuré : le taux pondérée/agressive sur `relief.json` va de 40/50 à 39/50 ; à rayon 4 il montait à 47/50, ce qui est le signe qu’un QG ouvert se prend.
2. **Les bâtiments désaffectés** (`MapDef.desaffectes` → `EtatPartie.desaffectes`, `doc/04` §6 bis). Le propriétaire demandait des « bâtiments en ruine » ; le mot est **interdit** par la charte du glossaire, comme « détruire », « ennemi » et « conquérir » — rien n’est détruit dans les Jeux Tactiques, seulement hors service. Un bâtiment désaffecté est neutre et ne sert à rien ; le **génie** (l’unité bâtisseuse) le remet en service en deux tours, l’infanterie en quatre. C’est une capture pour le moteur (même suite, même événement), un ordre « Remettre en service » pour le joueur, et une nouvelle silhouette pour le rendu. Il n’existe toujours aucun système de destruction : un bâtiment en service ne redevient jamais désaffecté.
3. **Le moteur passe en version 3** : les sauvegardes du 2 ne sont plus reprises.
4. **Catalogue 3 : drones, brouilleur, station radar** (`doc/04` §10 bis). Le brouillard de guerre existait déjà dans le moteur (§10) mais aucune mission ne l’activait ; le propriétaire le croyait absent. Le catalogue gagne un `drone` (3 000, brouillable), un `drone_filaire` (12 000, ne se brouille pas), un `brouilleur` mobile (5 000, dix cases) et un terrain `radar` (`T`, cinquième bâtiment capturable : vision 5, brouillage à douze cases). Un drone brouillé garde un dixième de sa vision. Une unité homologuée déclare désormais sa **version d’accueil** (`homologation.catalogue`) : `chargerCatalogue(2)` ne voit pas les drones, ce qui garde les rejeux du 2 exacts. Deux traits nouveaux, `drone` et `brouilleur` — la liste « fermée » compte treize traits, et `doc/04` §13.2 les décrit. Les rayons sont ceux demandés (12 et 10) : sur une carte de douze cases, une station couvre tout ; c’est dit dans le document, pas caché. L’exhibition `couleurs_alliees` est la vitrine : brouillard, un drone au joueur, une station et un brouilleur à Ariane.
5. **`npm run verifier:campagne` essaie trois joueurs** — une heuristique d’objectif, l’IA pondérée, l’IA agressive — et retient le premier qui gagne. Un seul joueur simple ne gagnait pas les six missions, et le propre des nouvelles missions est qu’elles demandent de se battre pour un objectif occupé. Ce n’est toujours pas une mesure de difficulté humaine.
6. **Le rendu, par trois agents en parallèle** : capture qui **hisse les couleurs** sur un mât permanent (le bâtiment ne s’aplatit plus quand une unité l’occupe : il devient translucide), **voies continues** par biome et vrais ponts (`textures-voies.ts`, `geometrie.ts` : `pieceDepuisLiaisons` est pure et testée), **paysages par biome** (`paysage.ts`, 54 accessoires, ligne de rivage). `/admin/assets` montre les 681 spécifications (541 → 681 avec le radar et les trois unités) ; aucune n’est livrée, et la page le dit.

7. **Trois suites demandées dans la foulée** : la remise en service verse une **prime** (1 000 fonds, 2 000 pour le génie — `PRIME_REMISE_EN_SERVICE`, l'événement la porte et le HUD l'annonce) ; un **drone mis hors jeu au-dessus d'un bâtiment adverse** émet `production_revelee` avec tout ce que ce camp a produit (`revelerProduction`, `combat.ts` — `mettreHorsJeu` prend désormais le catalogue) ; et le **lancer de rayon interroge les tabliers de pont avant le sol** (`Plateau.ponts`, `caseSous` accepte une liste de cibles), ce qui corrige le clic sous les ponts.

8. **Six demandes d'interface, par cinq agents en parallèle, puis l'atelier par trois** (6 septembre 2026, `doc/15` « Interface et rendu mobile ») : carte de démonstration de l'écran-titre refaite (île, ponts jumeaux, routes ; contrôle 29-31) et plateau SVG de repli qui dessine la vraie carte ; `/reglages` confirme l'effacement avec les chiffres et porte **deux profils** locaux (`src/app/preferences.ts` est la source de vérité des clés : A garde `atlas:qualification:v1` et `atlas:partie:*`, B prend `atlas:p2:*`, le profil actif est `atlas:profil:v1` ; le rendu reçoit une clé de sauvegarde déjà composée) ; **menu de production** en deux colonnes avec fiche (`bilanDegats` pure, `fiche.*`, `trait.*`, `mouvement.*` en i18n) ; **inspection** d'une unité adverse par double-clic ou appui long (`Controleur.inspecter`, `GestesRendu.surInspecter`, déplacements en `danger`, tir en `attaque`) ; unités **ayant joué** ternies, tassées, cadenas sur l'étiquette (`Materiaux.terni()`, restitution à l'identique) ; **caméra** : cadrage portrait sur la largeur et l'action, inertie avec butée, double-tap, zoom continu ×1,25 (`doc/10` §3.3 révisé) ; **`/atelier`** : dock repliable sur PC, feuille à onglets sur mobile, presets par biome, aides des gestes, vue dans l'URL (`encoderVue`/`decoderVue`), gestes brouillage et drone abattu. Le bouton rond « N » en bas à gauche des captures est l'indicateur de développement de Next, pas une boussole du jeu.

**Ce qui reste faux ou fragile** : `decor.ts` et `paysage.ts` lisent toujours la grille capturée au montage (le gel connu) ; les nouvelles unités n’ont qu’une silhouette procédurale et aucune ligne dans `content/degats.json` (elles portent leurs colonnes elles-mêmes, comme le génie) ; l’IA ne sait pas se servir d’un drone ni d’un brouilleur, elle les traite comme des transports.

## Mise à jour — cinq axes et entraînement (5 septembre 2026)

L’état historique ci-dessous doit être lu avec `doc/15-premiers-matchs.md` : six missions sont désormais jouables via `/campagne`, dont quatre tutoriels avant les matchs officiels. Objectifs escorte/relais/survie, génie, dix profils de biomes, styles de l’alliance Luxembourg et HUD adaptatif sont implémentés. Moteur 2, catalogue 2 (compatibilité catalogue 1), générateur 2. Les marées et le raccordement mapgen existaient déjà ; les passages ci-dessous disant le contraire sont obsolètes. La progression est locale au navigateur ; les routines Claude et le parcours Postgres ne sont pas mis en service par cette livraison.

## Mise à jour — grammaire Advance Wars (5 septembre 2026, soir)

Quatre changements d’interface, tous derrière l’interface `Rendu` commune, donc valables en 2D comme en 3D.

1. **Les commandants parlent sur la carte.** `Scenario.scenesDialogue` (`doc/03` §6) déclare des scènes déclenchées par les **événements** du moteur — première attaque, capture, perte, production d’un type d’unité, pouvoir, jalon de relais, journée. Les déclencheurs sont dans `src/render/dialogues.ts` (pur, testé), la boîte de dialogue dans `src/render/dialogue-html.ts` (buste vectoriel, frappe lettre à lettre, letterbox). `dialogueOuverture` se joue désormais **sur la carte une fois cadrée**, plus dans une modale ; la fiche de mission ne porte plus que le titre, l’objectif et le tutoriel. Le tour d’IA **attend** qu’une scène soit refermée. Option `dialogues` de `monterJeu`, **fausse par défaut** : la démo et les tests de fumée ne sont pas concernés.
2. **Vert, j’y vais ; rouge, j’y tire** (`doc/10` §8). Le déplacement passe au vert émeraude, et le rouge devient l’**enveloppe de tir complète** — toutes les cases frappables depuis n’importe quelle arrivée, privées des cases atteignables. L’or reste aux objectifs, le bleu aux chantiers du génie.
3. **Le chemin est une flèche**, coudée, avec pointe et liseré sombre, en 2D comme en 3D où elle épouse le relief.
4. **La prévision de duel** (`doc/04` §5.2 bis) : `prevoirDuel()` rejoue la formule de combat sans tirer d’aléa et sans rien muter ; le HUD montre les deux camps avant confirmation. Conséquence : même face à une cible unique, l’ordre `attaquer` passe par la phase de visée.

Au passage, `tests/schemas/contenu.test.ts` valide enfin `content/scenarios/` — rien ne le faisait —, et `e2e/fumee-3d.spec.ts` ouvre le Bulletin avant d’en lire les prévisions (il était devenu repliable, le test ne le savait pas).

## Mise à jour — drapeaux, villes entières et grille vivante (6 septembre 2026)

Trois retours dans la foulée de la fiche, et un gel qui a fini par mordre.

1. **La fiche parle comme Advance Wars.** Plus de mots là où un signe suffit : une botte, un œil, une cible, une balle pour les quatre statistiques (`iconeOrdre` a gagné cinq glyphes). La table de dégâts est une **rangée de figurines** — les vignettes du HUD, aux couleurs de l’adversaire —, le pourcentage sous chacune, vert quand c’est nous qui frappons, rouge quand c’est nous qui encaissons. Cadre à biseau, titres en rubans coupés en biais. Une seule feuille de règles, deux jeux de variables selon la surface.
2. **Les villes ne s’écrasent plus.** `majProprietaires` ramenait un bâtiment occupé à douze pour cent de sa hauteur pour laisser voir la figurine — une maquette qui ne se lisait plus comme une ville, et signalée comme telle. Le bâtiment devient **translucide** (`OPACITE_OCCUPE`, un clone de matériau par matériau, jamais par bâtiment ni par image ; chaque mesh fusionné garde `userData.matOrigine` pour le rendre ensuite) et cesse de porter ombre, sans quoi sa masse trahit sa transparence. La règle du brouillard tient : une unité cachée ne révèle rien.
3. **Le drapeau.** Le QG portait un mât et un pavillon ; ville, usine et aéroport n’avaient qu’un liseré au ras du sol, et après une capture le joueur cherchait **son drapeau** sans le trouver. Tout bâtiment possédé porte désormais le pavillon de son camp au coin du socle ; un bâtiment neutre n’en a pas, et c’est précisément ce qui le dit neutre. Pendant la capture, un groupe `chantiers` rebâti à chaque mise à jour pose un mât et un **fanion hissé à hauteur des points** (`pointsCapture / SEUIL_CAPTURE`) — la capture se regarde se faire, ce qui n’existait pas. Le geste « Capturer » du banc se joue en **deux temps** pour la montrer : l’unité monte sur la ville avec ses points à mi-chemin, puis l’emporte.
4. **Le décor repart de la grille courante.** Le gel noté plus haut — `construireBatiments` lisant la grille capturée au montage — a fini par mordre : sur le banc, un seul bâtiment sur onze, et l’infanterie montait sur de l’herbe nue. Arbres et rochers avaient le même défaut, avec en plus des maillages instanciés à capacité fixe. `Decor.majGrille(grille)` ressème les arbres et les pierres, **rebâtit** leurs maillages à la taille du nouveau semis, efface la signature des bâtiments pour qu’ils se rebâtissent au prochain `majProprietaires`, et repose le tout. `render3d/index.ts` l’appelle là où il appelait `majRelief` sur changement de terrain ; `majRelief` reste pour les images d’une mutation. Deuxième cause, dans l’atelier : `etatCourant` était mis à jour dans un effet qui court **après** celui du montage, lequel posait donc la scène avec l’état du monde précédent. Les références sont tenues à jour au rendu.

**Reste à surveiller** : `majGrille` recrée trois `InstancedMesh` d’arbres et trois de pierres à chaque changement de terrain, donc à chaque marée. C’est correct et c’est rare, mais sur une carte de soixante cases de côté cela vaudra une mesure.

## Mise à jour — la fiche d’unité (6 septembre 2026)

Le menu de production ne montrait qu’une silhouette, un nom et un prix. Acheter un char sans savoir ce qu’il chasse ni ce qui le chasse, c’est jouer à pile ou face — et tout ce qu’il fallait pour le dire était déjà dans le canon et dans le moteur, simplement affiché nulle part.

`src/render/fiche-unite.ts` (pur, testé) compose une **fiche** : ce qu’elle démolit et ce qui la démolit — la table de dégâts lue dans les deux sens, trois lignes de chaque côté —, les terrains qu’elle traverse au coût minimal, ceux qu’elle ne franchit pas, les météos qui la gênent, sa portée, sa vue, ses munitions, et un avertissement quand elle **tire de loin sans riposter au contact**, qui est la lecture qui change le plus une partie.

**Rien n’y est recopié**, et c’est la seule chose à préserver : les dégâts viennent de `degatsBase`, les coûts de `coutBase` — donc les traits `vol` et `tout_terrain` s’appliquent sans que la fiche les connaisse —, les malus de météo de `surcoutMeteo` et `facteurMouvementMeteo`. Ces deux dernières sont **nouvelles** : la moitié « météo » de `surcoutClimat` a été isolée en fonction pure pour qu’on puisse poser la question à l’avance — « cette unité avance-t-elle mal sous la pluie ? » — sans fabriquer un état de partie. Une règle d’équilibrage qui change au moteur change dans la fiche le jour même.

Dans le menu, un premier appui **déplie** la fiche et l’appel à l’action passe de « Production » à « Produire » ; le second achète. On informe sans coûter un geste à qui sait déjà ce qu’il veut. Au passage, un prix qu’on ne peut pas payer affiche « Fonds insuffisants » au lieu d’un nombre grisé — un bouton qui ne répond pas doit dire pourquoi —, et une unité trop chère reste **consultable** : on veut savoir pour quoi on économise.

**La mise en page de la fiche : titres pleine largeur, valeurs en pastilles.** La première version posait chaque titre en colonne de 106 px à gauche de ses valeurs. Sur un panneau de 390 px, « REDOUTABLE CONTRE » passait sur deux lignes et, dès que les valeurs débordaient, elles retombaient **sous le titre** au lieu de rester alignées — illisible, et signalé comme tel. Un bloc pose désormais son titre sur toute la largeur puis ses valeurs en pastilles, qui reviennent à la ligne sans rien casser ; les deux listes de terrain sont côte à côte parce qu’elles sont courtes ; et le chiffre d’un duel prend la couleur de ce qu’il signifie — vert quand c’est nous qui frappons, rouge quand c’est nous qui encaissons. Une seule feuille de règles de mise en page, deux jeux de variables de couleur selon la surface.

**Le panneau d’inspection ne suit plus le seul curseur.** Il l’ignorait complètement, ce qui rendait la fiche presque inatteignable : le bouton vit dans le panneau, en bas à gauche, donc pour l’atteindre la souris quitte l’unité et traverse des cases vides — et le bouton disparaissait avant qu’on l’ait touché. Trois sources, dans cet ordre : **l’unité sous le curseur** (inspecter l’adversaire qu’on s’apprête à frapper doit rester possible), puis **l’unité sélectionnée** (cliquer une unité doit afficher son détail, et c’est le seul chemin au doigt, où le survol n’existe pas), puis **la dernière montrée**, tant qu’on lit sa fiche, que le pointeur est sur un panneau du HUD, ou pendant un **sursis de 1,5 s** — le temps du trajet. Hors de ces cas le panneau suit le curseur comme avant : une case vide doit pouvoir montrer son terrain et sa défense.

Deux pièges rencontrés en chemin, à ne pas réintroduire. L’écoute du pointeur est posée sur le **conteneur**, pas sur la racine du HUD : celle-ci couvre tout l’écran, son `pointerleave` ne part jamais et le drapeau resterait vrai pour toujours. Et le HUD **remplace tout son DOM** à chaque rafraîchissement, ce qui rend `boundingBox()` de Playwright inutilisable sur ses boutons — un pilotage doit mesurer et cliquer depuis la page.

**La même fiche est sous le curseur.** Le panneau d’inspection porte un bouton qui la déplie ; `blocFiche` est partagé entre les deux endroits, parce que ce sont les mêmes questions et qu’il serait absurde qu’elles reçoivent deux réponses. Seules les couleurs changent, par le CSS : papier dans la modale de production, encre sous le curseur. Deux différences de comportement, et elles sont voulues. Le bouton **n’apparaît que sur une unité** — un terrain n’a pas de fiche, et une case vide ne doit pas offrir une commande qui ne ferait rien. Et la fiche **reste ouverte quand on change de case** : c’est une façon de jouer, pas un choix par unité. Qui apprend la laisse dépliée, qui connaît la referme une fois.

## Mise à jour — l’accueil ne clignote plus, et les documents sont alignés (6 septembre 2026)

Trois choses, dont deux visibles.

1. **Le fond de l’écran-titre ne change plus de nature en cours de route.** `vitrine.tsx` montrait le plateau SVG, puis l’attract mode par-dessus, en fondu. Un dessin à plat qui cède la place à un plateau en relief, ce sont deux jeux en une seconde et demie. Le choix se fait désormais **avant le premier pixel de fond** : animation réduite, réglage du joueur, WebGL 2 — les trois questions se posent au montage, et l’un **ou** l’autre s’affiche, jamais les deux. L’écran-titre reste lisible pendant ce temps, puisque le titre et le menu viennent du serveur.
2. **L’attract ne cadre plus lui-même.** Il appelait `recentrer` puis trois ou quatre crans de `zoomer(-1)` — des paliers réglés pour la caméra 2D, qui n’avait pas les mêmes. Or `creerRendu3d` appelle déjà `vue3d.cadrerCarte()` au montage, qui prend la **distance exacte** faisant tenir la carte. D’où le va-et-vient d’échelle. Le cadrage est rendu à la peau, et la caméra ne bouge plus **du tout** pendant l’exhibition : elle suivait chaque unité, ce qui donne le mal de mer sur une page qu’on ne fait que regarder.
3. **`next.config.ts` accepte `NEXT_DIST_DIR`.** `next build` et `next dev` écrivent tous les deux dans `.next` et se marchent dessus : construire pendant qu’un serveur de développement tourne le fait échouer, et oblige à couper le serveur qu’on est en train d’utiliser. `NEXT_DIST_DIR=.next-build npm run build` construit à côté. La valeur par défaut reste `.next` ; le `Dockerfile` et la production ne changent pas. `.next-build` est ignoré par git **et par eslint** — sans quoi le lint inspecte le bundle et rend dix mille avertissements.

**Les documents de conception ont été repris** dans la foulée, et le retard était de deux générations, pas d’une : `02-architecture.md` §2 justifiait encore le choix du Canvas 2D contre PixiJS, et §3.4 décrivait un dossier `render/` avec un cache de sprites et un `atlas.ts` qui n’existaient plus depuis le passage à la 3D. Même chose pour `09-i18n.md` §7.2 et §7.3, qui mesuraient les textes du HUD au `measureText`. Corrigés, avec la mention datée de ce qui a changé plutôt qu’une réécriture qui effacerait l’histoire : `BRIEF.md`, `PLAN.md`, `README.md`, `doc/README.md`, `00-vision`, `02-architecture`, `03-schemas`, `04-gameplay`, `05-routines`, `09-i18n`, `10-rendu-3d`.

## Mise à jour — une seule peau, et un banc d’essai (5 septembre 2026, nuit)

**Le rendu 2D vectoriel est supprimé.** `BRIEF.md` a été modifié en conséquence : le repli qu’il promettait n’existe plus, et un appareil sans WebGL 2 voit un écran qui le dit. Ont disparu : `rendu2d.ts`, `scene.ts`, `camera.ts`, `hidpi.ts`, `hud.ts` (le HUD dessiné au canvas), quatre des six fichiers de `sprites/`, deux fichiers de tests et `e2e/fumee.spec.ts`. Trois choses en sont sorties plutôt que d’être perdues :

- `src/render/surbrillance.ts` — le **vocabulaire** des cinq genres (`deplacement`, `attaque`, `capture`, `production`, `danger`). Le contrôleur, les objectifs et la peau 3D en avaient besoin, mais aucun d’eux n’a à savoir comment on les peint.
- `src/render/libelles.ts` — les noms d’unités, de terrains, de commandants, de saisons et de météos. C’est tout ce que le HUD HTML utilisait de l’ancien HUD canvas.
- `src/render/sprites/` — réduit à `formes.ts` et `silhouettes.ts`, gardés pour la **vignette d’unité** du HUD. Ce n’est plus du rendu de carte, c’est de l’iconographie d’interface.

L’interface `Rendu` **subsiste** : c’est elle qui empêche `render/` d’importer `render3d/` (`02-architecture.md` §5). `monterJeu` ne choisit plus, il exige une `fabriqueRendu` et **lève** si elle manque ou refuse de se monter. `?rendu=`, `choisirRendu`, `preferenceDe`, `PreferenceRendu` et le réglage « Affichage » ont disparu avec elle ; `CleRendu` ne vaut plus que `'3d'`. L’attract mode de l’accueil passe en 3D et ne se monte pas sans WebGL 2 — **il télécharge 384 ko de JS au lieu de 230**, à surveiller.

**L’atelier est devenu un banc d’essai** (`src/app/atelier/banc.ts`, pur et testé). Il montrait trois cartes de mission en 3D, sans surbrillances ni animations : les défauts qu’on y cherche étaient précisément ceux qu’il ne pouvait pas montrer. Il porte désormais une **carte-catalogue** — les douze terrains côte à côte, les quatre bâtiments pris par chaque camp et neutres, les onze unités dans les deux camps, et les cas qui ont réellement cassé (un bâtiment encastré entre deux montagnes, une plage entre mer et plaine) —, les cinq surbrillances, la flèche, le brouillard, le pays de chaque camp parmi les vingt-quatre, six gestes rejouables (déplacer, tirer, capturer, mettre hors jeu, marée haute et basse) et un bouton qui fait apparaître les silhouettes **`coque`, `ailes` et `rail`**, écrites dans `pieces.ts` depuis le début et jamais vues à l’écran.

Le banc a trouvé deux défauts dans l’heure qui a suivi, et c’est son intérêt :

1. **`majTerrain` débordait sur un changement de taille de carte.** `(splat.image.data).set(donnees)` levait un `RangeError` qui blanchissait la page dès qu’on passait d’une carte 16×12 à une 20×12. Le commentaire disait « les dimensions ne changent jamais en cours de partie » — c’est vrai d’une partie, faux d’un plateau qui survit à un changement de carte. La texture est maintenant **remplacée** quand la taille change, au lieu d’être remplie.
2. **Le banc perdait le génie en silence.** Le scénario de démonstration est en **catalogue 1**, qui ne compte que dix unités. Le banc force désormais le catalogue 2, et un test échoue si la version choisie ne porte pas toutes les unités qu’il pose.

**Reste à faire, trouvé au passage** : `construireBatiments` (`render3d/decor.ts`) lit la grille **capturée au montage**, pas celle de l’état courant. Une marée qui noierait une ville laisserait la ville flotter. C’est la même faute que le terrain gelé au premier jour, au même endroit du raisonnement — toute donnée dérivée de la grille et calculée au montage est un gel en puissance.

## Mise à jour — l’écran-titre et les réglages (5 septembre 2026, nuit)

`src/app/page.tsx` n’est plus une page de présentation : c’est un **écran-titre de jeu, pensé en portrait d’abord**. La version précédente mesurée sur 390 px plaçait son seul bouton à 711 px du haut et le plateau — la seule chose qui montre le jeu — à 805 px : deux écrans plus bas. Quatre décisions le tiennent, et elles valent pour la suite du site.

1. **Le jeu est le fond, en plein cadre.** L’attract mode (`attract.tsx`) et son repli SVG (`plateau-accueil.tsx`) passent en `position:fixed;inset:0` derrière tout, cadrés en `slice` pour que le fondu entre les deux ne fasse pas sauter l’échelle. Le cadre de la vitrine disparaît au profit d’une **nappe en dégradé** plus d’une vignette : on n’encadre pas une capture de son propre jeu sur son écran-titre. L’attract est désaturé (`saturate(.86) brightness(.82)`) pour que le vert et le rouge du jeu ne disputent pas au signal le rôle de « couleur qui appelle ». Le coût reste cantonné : l’accueil pèse **2,57 ko / 109 ko**, l’attract n’entre jamais dans le rendu initial.
2. **Quatre boutons, ancrés en bas** — Campagne, Jeu libre, Atelier des mondes, Réglages. Au-delà de 560 px du bord inférieur, un pouce n’atteint plus rien : le titre y va, le menu jamais. Mesuré, le bouton Campagne est à 281 px du bas en 844 comme en 667. L’ancrage se fait par `margin-top:auto`, **jamais `position:fixed`** — la rétraction de la barre d’adresse iOS fait sauter un élément fixe. Cinq registres distinguent les boutons et aucun n’est le texte : peinture, hauteur, biseau, ombre, glyphe.
3. **Campagne porte l’état, et le geste unique survit.** Le bouton mène droit à la prochaine épreuve non remportée et affiche sur lui-même la jauge de six segments et le titre de l’épreuve ; la carte de progression entière est absorbée dedans. Une seconde cible, « Carnet », mène au choix libre — sans elle, rejouer une mission depuis l’accueil devient impossible. Le serveur rend l’état neutre (jauge vide, aucun chiffre, destination `/campagne`, vraie dans tous les cas), le client l’enrichit : afficher « 0 sur 6 » deux cents millisecondes à quelqu’un qui a tout gagné serait une affirmation fausse.
4. **Un écran, pas de défilement** — vérifié à 390 × 844, 375 × 667 et 1280 × 800. Ce qui ne tenait pas a été **supprimé**, pas repoussé : le bandeau de marque, le pitch de 190 caractères, les trois liens soulignés, le pied et la frise des vingt-quatre nations. Mais `overflow:hidden` a disparu de `.atlas-accueil`, et `100dvh` est devenu `100svh` : la page ne défile pas parce qu’elle ne dépasse pas, jamais parce qu’on le lui interdit — à 200 % de zoom texte, l’interdit coupait le bas sans recours.

**`/reglages` existe** (`src/app/reglages/`, plus `src/app/preferences.ts`), et ne porte que les réglages qui existent réellement dans le code : les dialogues des commandants et la réduction des animations. Il portait aussi le choix de la peau ; celui-ci a disparu avec le rendu 2D, quelques heures plus tard. Pas d’interrupteur inerte : il n’y a pas d’audio dans le jeu, il n’y a donc pas de réglage de son. Le réglage système reste **maître** sur les animations — l’interrupteur ne peut qu’ajouter la réduction. « Effacer ma progression » emporte `atlas:qualification:v1` **et** tous les `atlas:partie:*` — n’effacer que le premier laisserait des parties fantômes reprenant au milieu d’une épreuve qu’on croit n’avoir jamais commencée. `preferences.ts` **recopie** `PREFIXE_SAUVEGARDE` plutôt que d’importer `render/jeu.ts`, qui ferait entrer le moteur et le rendu dans la page ; `tests/campagne/preferences.test.ts` échoue si les deux divergent, et c’est ce qui rend la copie acceptable — la page pèse 1,2 ko.

L’attract mode a été repris au passage : cadrage serré au lieu de la carte entière, rythme accéléré (330 ms d’intention, 190 ms entre deux actions), caméra qui **suit l’action** par `cadrer()` — qui ne recentre que si la case sort du champ —, et une graine tirée parmi cinq à chaque visite. Chaque partie reste déterministe, c’est le moteur ; mais un écran-titre qui repasse le même match coup pour coup se remarque dès la deuxième ouverture.


## Mise à jour — le terrain qui bouge (5 septembre 2026, nuit)

Les deux rendus **gelaient le terrain au premier jour**. En 3D, la grille était capturée dans une fermeture au montage de `batir()` ; en 2D, la couche de fond était mise en cache sous une clé qui ignorait la journée. Or une mécanique régionale — les marées — **réinterprète** la grille sans jamais l’écrire (`modifTerrain` est une lecture) : la marée changeait dans le moteur et jamais à l’écran. Le génie, qui pose et retire du terrain, souffrait du même gel.

Trois corrections, et une leçon.

1. `Plateau.majTerrain(grille)` remet en place altitudes, splat et routes **sans reconstruire** le plateau : rebâtir coûterait la repeinture des cinq jeux de matières et un recadrage de caméra à chaque journée.
2. La clé du cache 2D et le témoin 3D utilisent `signatureTerrain` — la fonction **du moteur** (`src/engine/hooks.ts`), qui porte déjà journée, climat, terrains posés et état de la mécanique. Une signature réinventée côté rendu serait forcément plus pauvre.
3. **Ce qui repose sur le sol doit se reposer avec lui.** Les unités relisent l’altitude à chaque `maj`, mais le décor était posé une fois pour toutes : `Decor.majRelief()` replace arbres et rochers après une mutation. Les unités s’**orientent** en plus sur la pente (bornée à 13°, suivie à 55 % — suivre le relief au degré près fait culbuter un char sur une berge) et ne descendent jamais sous le plan d’eau : une pièce reprise par la marée patauge, elle ne se noie pas.

4. **Le centre de chaque case est enfin plat.** `10-rendu-3d.md` §4.2 l’exige depuis le début — « sinon une unité posée sur une pente penche et le décalque de surbrillance se déforme » — mais `hauteurEn` était une interpolation bilinéaire pure : la pente traversait le centre de la case. Une maison posée sur une montagne s’enfonçait d’un côté et flottait de l’autre. Une rampe plate sur ±0,28 case reporte le dénivelé **sur la jonction** entre cases ; le plateau y gagne des gradins de jeu plutôt qu’une dune. **Fait depuis** : le second garde-fou de la même règle, « les cases de bâtiment sont plates, elles et leur couronne immédiate ». `REPLI_CENTRE` valait 0,28 pour toutes les cases, or le socle d’un bâtiment fait 0,83 de côté et son liseré de camp va jusqu’à ±0,46 : il débordait du disque plat et se faisait couper par le relief voisin — « la maison est dans la montagne ». Une case bâtie a désormais un repli de **0,5**, donc plate d’un bord à l’autre, et `rampeEntre(t, repliA, repliB)` accepte **deux replis différents** : un bâtiment impose son plateau jusqu’à sa propre frontière sans écraser le relief de sa voisine, qui garde toute sa hauteur dès son côté de la jonction. Aplanir la voisine aurait effacé un relief qui coûte du mouvement et donne de la défense, donc menti sur les règles. Le maillage ayant trois subdivisions par case, la marche tient exactement dans un quad.

7. **Les pierres cessent d’être des dés.** C’était un `DodecahedronGeometry` unique, gris uni, tourné sur trois axes au hasard et **relevé** de 0,06 au-dessus du sol : une caillasse de dés flottant chacun sur une facette d’appui. Trois silhouettes désormais — un bloc, des éclats, une dalle —, chacune érodée par un bruit tiré de la **position arrondie** de ses sommets, faute de quoi les facettes se décousent (la géométrie d’un icosaèdre n’est pas indexée). Elles tournent librement autour de la verticale, s’inclinent à peine, **s’enfoncent** au lieu de se poser, portent une teinte par instance, et se sèment en couronne pour laisser le centre de la case à l’unité qui s’y pose. Un appel de dessin par silhouette, et le placement reste dérivé de l’aléa de case.
5. Une mutation de terrain est devenue un **événement** de 1,4 s : fondu du mélange de matières, glissement des altitudes, écume qui enfle puis retombe. Les normales ne sont recalculées qu’à la fin — les rafraîchir à chaque image coûterait plus que tout le reste pour un gain invisible.

6. **Le rendu n’invente plus le trajet d’un déplacement.** L’événement `deplacement` ne portait que `de` et `vers` : la peau 3D reconstruisait un chemin **en L** et la 2D glissait en **ligne droite**. Les deux traversaient montagnes et unités adverses. L’événement porte désormais `chemin`, celui que `verifierChemin` a validé, tronqué à la case d’arrêt en cas d’interruption sous brouillard. `cheminEnL`, `longueurChemin` et `surChemin` ont quitté `render3d/` pour `src/render/chemin.ts` : les deux peaux en avaient besoin, et `render/` n’a pas le droit d’importer `render3d/` (`02-architecture.md` §5).

**La leçon, pour la suite** : toute donnée dérivée de la grille et calculée au montage est un gel en puissance. Le décor, l’éclairage et les surbrillances lisent `plateau.hauteurEn`, qui est désormais une fermeture **vivante** ; ne pas la remplacer par une valeur.

**Le bandeau de tour attend la fin d’une scène.** Un « à vous de jouer » et une réplique de commandant lancés au même instant se disputent la même seconde ; `annoncerTour` efface le tour déjà annoncé quand une scène est ouverte, de sorte que l’annonce se rejoue juste après la dernière réplique. Les deux arrivent, l’un après l’autre.

**L’entrée en mission est directe** (`src/app/jeu/[scenario]/toile.tsx`) : plus de fiche à valider avant de jouer. Le plateau se monte tout de suite, une partie en cours se reprend d’elle-même, et l’ouverture se joue en dialogue sur la carte. L’objectif est affiché en permanence dans le rappel de mission, en haut à gauche ; le tutoriel, le conseil et « nouvelle partie » sont derrière ce rappel.

Seule `src/app/convocation.tsx` est cliente, parce qu’elle lit `localStorage` ; la page lui passe des **libellés déjà traduits**, comme on le fait pour les rendus. Le sens de l’hydratation est à sens unique : le serveur rend l’état neutre, le client l’enrichit — jamais l’inverse, une bascule « reprendre » → « entrer » se lirait comme une progression perdue.

## Le projet en cinq lignes

Atlas Tournament est un tactique au tour par tour dans l'esprit d'Advance Wars, jouable dans un navigateur. Dans ce monde, les guerres ont été remplacées par des Jeux Tactiques : chaque pays a une équipe et un commandant, et un tournoi fait le tour de la planète tous les quatre ans. Le joueur part de France — tout le monde part de France —, traverse ses 18 régions puis le monde, et ses choix décident de la fin qu'il obtient et de l'état des 24 nations : alliée, rivale, retirée. Une nation alliée peut être **incarnée** — jouée entièrement, avec son général et son catalogue, le temps d'un match — et s'ouvre comme départ pour une Nouvelle Ronde. Techniquement : une seule application Next.js 15 en TypeScript strict, un moteur de règles pur et déterministe, un rendu 3D three.js, et cinq routines Claude qui produisent le contenu sous le contrôle d'un serveur qui ne fait confiance à rien. Rien de généré ne passe en ligne sans un verdict mesuré, et souvent sans un humain.

## À lire d'abord, dans cet ordre

1. **`BRIEF.md`** — le canon. Toutes les décisions prises, y compris les arbitrages du 5 septembre 2026.
2. **`PLAN.md`** — les treize étapes (0 à 12), avec un critère de fin écrit pour chacune.
3. **`doc/README.md`** — l'ordre de lecture des quinze documents de conception, et surtout **qui est propriétaire de quoi**.

**La règle de résolution des conflits, sans exception : le brief a raison sur tous les documents ; le document propriétaire d'un sujet a raison sur tous les autres documents.** `doc/README.md` donne le tableau des propriétaires. Exemple concret : si `PLAN.md` et `doc/11-assets-spec.md` ne disent pas le même nombre de spécifications d'assets, c'est `doc/11` qui fait foi, parce qu'il est propriétaire des assets.

Un document ne se lit **jamais** comme les autres : `doc/14-secrets.md`. Il n'est jamais servi à une routine, jamais résumé dans un prompt, jamais copié dans `content/`.

## L'arborescence

```
src/app/          Next.js App Router. La seule couche qui a le droit de tout importer.
                  Pages : `/` (accueil provisoire), `/jeu/[scenario]`, `/admin/*`.
                  API : /api/health, /api/canon, /api/i18n, /api/admin, /api/routines/*,
                  /api/modeles (l'inventaire des .glb livrés, lu une fois par page par le rendu).
src/engine/       Le moteur, pur et déterministe : (état, action) → état. Règles, RNG seedé,
                  rejeu, climat, mécaniques régionales par hooks, déblocages. Zéro dépendance.
src/ai/           L'IA de jeu : une fonction d'évaluation et trois stratégies (pondérée,
                  agressive, défensive). C'est elle qui certifie les cartes.
src/mapgen/       Le générateur : ParametresCarte + graine → MapDef, symétrie, vérifications,
                  aperçu texte. Deux appels avec la même graine donnent la même carte.
src/render/       Le socle de rendu, sans une ligne de three.js : interface `Rendu`, boucle
                  paresseuse, entrées, HUD HTML, ambiance, libellés, surbrillances, sauvegarde.
src/render3d/     La peau 3D three.js : scène, terrain, éclairage par saison/phase/météo, unités
                  et décor instanciés, surbrillances, animations, textures.
src/assets/       Le format `AssetSpec`, le catalogue qui le compose depuis le canon, les styles
                  nationaux, et les validateurs (spec et glTF binaire, sans dépendance).
src/content/      Les chargeurs typés du canon JSON.
src/schemas/      Les types partagés et les validateurs écrits à la main, sans dépendance.
src/i18n/         `t()`, l'ordre de repli des langues, et la table des chaînes d'interface source.
src/db/           Drizzle : le schéma, le client, et un fichier de requêtes typées par table.
src/serveur/      La logique serveur testable en Node : auth, cycle de contenu, file de missions,
                  contrôle, simulation, dépêche, traduction, canon, prompts, sonde.

content/          Le canon JSON, servi en lecture seule par /api/canon.
                  unites, terrains, degats, mecaniques, archetypes, flags, gabarits-missions ;
                  pays/ (24), regions/fr/ (18), styles/ (24 + regions/fr/ 18), fils/ (9),
                  cartes/, scenarios/, i18n/ (chaînes et glossaire français).
assets/specs/     681 AssetSpec générées depuis le canon, versionnées.
scripts/          migrate.mjs, simuler.ts, controler-carte.ts, apercu-carte.ts,
                  generer-specs-assets.ts, extraire-chaines.ts.
drizzle/          Les migrations SQL numérotées. Un fichier appliqué ne se modifie jamais.
tests/            tsx --test, un dossier par couche, plus frontieres.test.ts.
e2e/              Deux tests de fumée Playwright (2D et 3D), sur le port 3400.
doc/              Les quinze documents de conception, plus doc/assets/ (démos de rendu).
apercus/          Les PNG de relecture produits par apercu-carte.ts. Ignoré par git.
```

## Les règles d'import

`tests/frontieres.test.ts` les fait respecter mécaniquement : il lit les imports de chaque fichier de `src/` et échoue sur la moindre violation. La table des couches autorisées est **dans le test**, et c'est elle qui fait foi.

- `engine`, `ai`, `mapgen` ne connaissent que `schemas` et `content`. Jamais `render`, jamais `db`, jamais `app`.
- `render` peut importer `engine`, `schemas`, `content`, `i18n`. `render3d` ajoute `render` et `assets` — jamais l'inverse. C'est cette règle, et elle seule, qui justifie que l'interface `Rendu` survive à la disparition de la seconde peau.
- `serveur` peut importer le jeu et `db` ; `app` peut tout importer ; `schemas` n'importe rien.

**Les interdits dans les couches pures** (`engine`, `ai`, `mapgen`, `schemas`, `content`) sont vérifiés par le même test, par simple recherche de motif : `window`, `document`, `fetch(`, `Date.now(`, `Math.random(`. Le moteur ne lit jamais l'horloge — pas même pour évaluer un déblocage : la date lui est **donnée** par l'appelant, et c'est le serveur qui la fixe. Le hasard passe exclusivement par le RNG seedé de `src/engine/rng.ts` et ses flux dérivés. `render/` et `render3d/` ont le droit d'appeler `Date.now()`, et seulement pour cadencer une animation.

## Les conventions

- **Tout est en français** : le code, les commentaires, les noms de fichiers, les identifiants. Un commentaire dit *pourquoi*, pas *quoi*.
- **Les clés sont en minuscules sans accent**, avec des tirets bas : `carte_plaine_symetrique`, `pays.fr.qualifie`, `hud.fin_de_tour`. Les regex qui les valident sont dans `src/schemas/types.ts` et testées par `tests/schemas/regex.test.ts`.
- **Le moteur ne connaît aucune unité par son nom.** Les dix unités sont des données (`content/unites.json`) avec leurs traits et leur `Silhouette` ; une unité homologuée demain doit fonctionner sans une ligne de code moteur.
- **Aucun texte en dur dans le rendu.** Tout libellé passe par une clé et `t()`. Un rendu n'appelle même pas `t()` : on lui donne les rares libellés déjà traduits. `npm run extraire-chaines` reconstruit `content/i18n/` et un test échoue sur tout littéral affichable trouvé hors d'un appel à `t()`.
- **Tout contenu est validé par `src/schemas/valider.ts`**, à l'entrée du serveur comme au chargement du canon. Ces validateurs sont écrits à la main, sans aucune dépendance, et ils sont la seule porte : le serveur recalcule et refuse ce qu'il n'a pas lui-même soumis.
- **Déterminisme** : même graine + mêmes actions = même état, au bit près. C'est ce qui permet à la routine contrôle de certifier une carte, et c'est le premier invariant à ne pas casser.
- **On ne vérifie jamais à l'œil, seulement par du code.** Consigne du propriétaire (6 septembre 2026) : pas de capture d'écran, pas de relecture visuelle d'une page ou d'un rendu pour se convaincre qu'une chose marche. Une vérification passe par un test unitaire (`tests/`, `tsx --test`), un test de fumée écrit (`e2e/`), `npm run typecheck`, `npm run lint` ou `npm run build`. Ce qui ne se vérifie pas par du code se décrit comme non vérifié.

## Les commandes

| Commande | Ce qu'elle fait |
|---|---|
| `npm run dev` | le site en développement, **port 3400** |
| `npm run build` | `next build` — n'exige **aucune** variable d'environnement |
| `npm start` | migrations puis Next (c'est ce que lance le Dockerfile) |
| `npm run typecheck` | `tsc --noEmit`, TypeScript strict |
| `npm run lint` | ESLint |
| `npm test` | les tests `tsx --test`, un dossier par couche — un sous-ensemble se lance par `node --import tsx --import ./tests/aides/webgpu-en-node.mjs --test <fichiers>` (jamais `npx tsx --test` seul : deux copies de three) |
| `npm run test:e2e` | le spec Playwright 3D, avec ses propres drapeaux SwiftShader |
| `npm run migrate` | applique `drizzle/*.sql` une fois chacun, copie de sécurité `pg_dump` avant |
| `npm run simuler -- --carte tests/engine/cartes/plaine.json --parties 50 --graine 1` | N parties IA contre IA |
| `npm run controler -- --carte <fichier>` | le verdict exact de la routine contrôle, hors ligne |
| `npm run controler:asset -- --spec assets/specs/<id>.json --glb <fichier.glb> [--fichiers <dossier>] [--json]` | le verdict `{ ok, motifs }` d'un asset livré, celui que la routine contrôle rendrait, hors ligne ; code 1 sur refus |
| `npm run generer:infanterie [-- --sec] [-- --sortie <dossier>]` | produit l'infanterie de base (trois GLB riggés et animés, quatre PNG) de façon déterministe, la contrôle comme `controler:asset`, et la dépose dans `public/assets/modeles/` si les trois niveaux sont acceptés ; `--sec` ne dépose rien |
| `npm run apercu -- --params '{"largeur":16,"hauteur":12}' --graine 7 --sortie apercus/c.png` | une carte en PNG et en texte |
| `npx tsx scripts/generer-specs-assets.ts --verifier` | vérifie que `assets/specs/` n'a pas dérivé du canon |
| `npm run extraire-chaines` | extrait les chaînes ; marche à blanc sans `DATABASE_URL` |

## L'état réel, étape par étape

| Étape | État | Ce qui est là, ce qui manque |
|---|---|---|
| 0 — Socle | **fait** | Next 15, TS strict, `tsx --test`, Playwright, Drizzle, `migrate.mjs`, `.env.example`, CI. La CI ne lance **ni `lint` ni Playwright**. |
| 1 — Moteur | **fait** | Règles, RNG, rejeu, climat, hooks de mécaniques, IA à trois stratégies, `simuler.ts`. |
| 2 — Générateur + contrôle | **fait** | `mapgen/`, vérifications, campagne multi-climats, `ReviewVerdict` motivé, `controler-carte.ts`, `apercu-carte.ts`. La campagne des 200 cartes aléatoires du critère de fin n'a pas été relue à l'œil. |
| 3 — Rendu | **fait** | Interface `Rendu`, peau 3D, HUD HTML, un spec de fumée. La peau 2D et le repli WebGL ont été **retirés** (voir ci-dessus). Depuis le 7 septembre 2026 au soir, le moteur est **WebGPU** (`WebGPURenderer`, repli WebGL 2 par le même moteur). Le seuil de 48 px par case n'est pas exercé sur tous les couples (carte × écran). |
| 4 — Assets 3D | **partiel** | 681 specs générées et valides, validateur glTF écrit. **Aucun `.glb` réel n'existe** : tout est placeholder. La boucle avec le générateur externe n'a jamais été parcourue. |
| 5 — Serveur et admin | **partiel** | Toutes les routes, le cycle, les prompts versionnés, la file, l'admin, la sonde — **écrits et testés hors base**. Aucune base n'a jamais été branchée. |
| 6 — Routines en ligne + i18n | **à faire** | Aucune tâche planifiée Claude n'existe. Côté i18n, seul `fr` a un bundle ; `en` est déclaré en base par la migration mais **aucun bundle `en` n'existe**. |
| 7 — France, 18 régions | **partiel** | Les 18 fiches sont dans `content/regions/fr/` et leurs styles aussi. **Aucune mécanique régionale n'est implémentée dans le moteur** : il n'y a que le contrat de hooks et une mécanique de test. |
| 8 — Lore et 24 nations | **partiel** | Les 24 fiches `Country` sont dans `content/pays/`. Pas de prologue, pas de routine lore, rien en base. Pas d'écran de choix du pays, et il n'en faut pas au premier parcours : tout le monde part de France (`BRIEF.md`, révision du 5 septembre au soir) ; les 24 fiches sont des **relations**, puis des départs débloqués en Nouvelle Ronde. |
| 9 — Voyage et campagne | **partiel** | `deblocages.ts` (dix conditions, `confiance` comprise), `content/fils/` (9 fils), `content/gabarits-missions.json`, le type `ProfilCampagne` et son validateur. Les **matchs d'incarnation** existent au schéma (`Scenario.incarnation`) et dans le moteur (`sceneDepuis` place le général incarné au camp du joueur) ; le **co-commandant passif** et la **montée de la confiance** ne sont pas implémentés — c'est le serveur qui devra recalculer `confiance` comme il recalcule `relations`. Pas de carte du monde, pas de carnet, pas de persistance de profil, aucune mission écrite. |
| 10 — Cerveau et jeu vivant | **partiel** | Les routes et la logique de dépêche, d'homologation, de mémoire et de prompts existent. Rien n'a tourné. **La mise en ligne à 18 h n'est pas câblée** (voir ci-dessous). |
| 11 — Paquet naval | **entamé** | Catalogues 5 et 6 (7 septembre 2026) : quatre navires, le port, le chasseur furtif, deux cartes libres, des ports dans le générateur. Restent le récif, la haute mer, les mécaniques et les spécialités maritimes ; huit pays gardent leur spécialité de repli terrestre. |
| 12 — Finition | **à faire** | Rien. Huit des neuf langues restent `en_preparation`. |

## Les manques connus, nommément

Ils sont listés ici parce qu'ils se voient mal dans le code, pas parce qu'ils sont graves.

1. **Aucune base n'est branchée ni testée en réel.** Tout `src/db/` et une bonne partie de `src/serveur/` n'ont jamais parlé à un Postgres. Le test `tests/serveur/migrations.test.ts` est **sauté** faute de `DATABASE_URL` (c'est le seul des 629 qui l'est). `npm run migrate` sans base va proprement jusqu'à `ECONNREFUSED` et rend 1.
2. **`scripts/migrate.mjs` ne lit pas `.env`.** Il attend `DATABASE_URL` dans l'environnement, ce qui est juste en production (Coolify l'injecte) mais surprend en local : `npm run migrate` répondra `DATABASE_URL manquante` même avec un `.env` rempli. Lancer `node --env-file=.env scripts/migrate.mjs`, ou ajouter le drapeau au script — c'est une décision à prendre, pas un oubli à corriger en silence.
3. **La mise en ligne de la Dépêche à 18 h n'est pas câblée.** `armer()` passe la dépêche en `valide` sur décision humaine, et la requête `enLigne()` ne sert que le statut `en_ligne` : **rien ne fait la transition à l'heure dite**. Il manque le déclencheur (huitième tâche planifiée, ou évaluation paresseuse à la lecture — à trancher).
4. **Aucun bundle `en`.** `src/i18n/` ne connaît que `SOURCE_FR`. Les huit autres langues sont des lignes en base, sans glossaire ni traduction. Le repli `langue → en → fr` fonctionne, mais il tombe toujours sur `fr`.
5. **Aucun modèle 3D réel.** Les unités, terrains, bâtiments et décors sont des placeholders composés depuis la `Silhouette`. Depuis le 6 septembre au soir, le chargeur est prêt à en recevoir un (rotation, gabarit, LOD, masque, mixer, commande de contrôle) : ce qui manque est le fichier, pas le code. Depuis le catalogue 5, `coque` et `ailes` habillent neuf unités, et l'aile delta (`ailes` + `plateau`) le chasseur furtif du 6 ; seule **`rail`** n'habille encore personne.
6. **L'IA va souvent aux points** — nettement moins depuis le 7 septembre 2026 (16 → 5 `limite_journees` sur 20 parties de plaine ; voir « le cerveau logistique »). Le soir même : plaine 40/60 et `limite_journees` 5, mais **sur l'archipel 20 parties sur 20 au chronomètre** — c'est là que le manque vit désormais. Sur 50 parties de `plaine.json`, 28 finissent par `limite_journees`, donc par une décision aux points, contre 22 par élimination. C'est jouable et déterministe, mais ce n'est pas une IA qui cherche à gagner : elle capture et échange, et laisse le chronomètre trancher.
7. **Clos le 7 septembre 2026 au soir.** `scripts/simuler.ts` compte désormais « sans vainqueur » comme `src/serveur/controle/verdict.ts` ; les décisions aux points se lisent dans les motifs de fin (`limite_journees`), plus dans ce compte.
8. **Le moteur ignore trois mécaniques promises par les documents** : les **cartes de terrain** (`doc/04` §7.5), les **co-commandants** (passif seul + barre de jauge, y compris le commandant d'origine du joueur pendant un **match d'incarnation**, et la jauge entière d'un général à confiance 3) et les **spécialités**. Elles existent dans `src/schemas/` — types et validateurs —, pas dans `src/engine/`. De l'incarnation, le moteur ne tient aujourd'hui qu'une chose, et c'est la bonne : le camp du joueur prend le général incarné, et le catalogue joué est celui que l'appelant lui passe.
9. **Les 24 unités spéciales n'ont aucune spécification.** Ce sont des modèles uniques, pas des kits ; il faut d'abord qu'elles entrent au catalogue d'unités (`doc/11` §10.3).
10. **Le validateur glTF ne vérifie pas les textures livrées** : il voit qu'une carte obligatoire est présente, pas sa résolution ni le caractère binaire du masque d'équipe (`doc/11` §10.4).
11. **Les assets de type `effet`** (impacts, poussière, halo de pouvoir) existent comme type, sans aucune spécification produite.
12. **Le générateur de cartes n'est pas branché sur le serveur** : `src/serveur/generation.ts` et la partie catalogue de `src/serveur/simulation.ts` lèvent une erreur « pas encore branché ».
13. **L'accueil ne mène pas encore au monde.** L'écran-titre est fait (ci-dessus), mais il n'y a toujours ni choix du pays, ni carte du monde — le carnet, lui, existe. Et « Atelier des mondes » promet un éditeur alors que c'est un **visualiseur** de biomes, saisons et météo : sa sous-ligne le dit, le nom continue de mentir.

## Les prochaines actions, dans l'ordre

1. **Brancher la base Coolify.** `cp .env.example .env`, y mettre la vraie `DATABASE_URL` — elle n'est **jamais** commitée, `.env` est dans `.gitignore` —, plus `ADMIN_PASSWORD`, `AUTH_SECRET` (`openssl rand -hex 32`), `CRON_SECRET` et `SITE_URL`.
2. **Premier `npm run migrate`**, puis un second immédiatement : la migration doit être rejouable sans rien faire. Vérifier `/api/health` en `200`, et que `tests/serveur/migrations.test.ts` cesse d'être sauté.
3. **Créer les tâches planifiées Claude** avec les prompts bootstrap prêts à coller : `doc/05-routines.md` §2.6 (contrôle), §3.6 (map), §4.6 (lore), §5.9 (cerveau), et `doc/09-i18n.md` §8.7 (traduction). Le récapitulatif des sept tâches et de leurs crons UTC est en `doc/05` §7.1. Commencer par `atlas_map` et `atlas_controle` seuls, une semaine, et regarder le taux de rejet par motif avant d'ouvrir les autres.
4. **Semer les 24 pays en base** depuis `content/pays/*.json`, avec les 18 régions et les styles. Il n'existe aucun script pour cela : c'est un `scripts/semer.ts` à écrire, qui valide chaque fiche par `src/schemas/valider.ts` avant l'insertion, et qui est **rejouable**.
5. **Ouvrir `en`** : écrire `content/i18n/glossaire.en.json` (sans lui, le serveur ne sert aucun lot), lancer `npm run extraire-chaines` avec la base, laisser `atlas_traduction` remplir la file, relire l'échantillon humain.
6. **Câbler la mise en ligne de 18 h** (manque n° 3) et faire le test qui fait rater une échéance : le vide vaut mieux qu'une erreur.
7. **Passer le rendu 3D sur de vraies textures.** Envoyer au générateur externe les **premières specs par priorité 1** — les 165 qui suffisent à jouer la qualification française : terrains d'abord (ils couvrent l'écran), puis bâtiments, puis décor, puis unités, puis bustes. Un asset refusé ne remplace **jamais** son placeholder. Après chaque famille livrée, une passe de performance contre le budget de `doc/10` §9.2.
8. **Ensuite seulement**, les mécaniques régionales françaises (étape 7), qui sont le premier gros morceau de code moteur restant.

## Le déploiement

Coolify, image construite depuis le `Dockerfile` à la racine (Node 22 Alpine, trois étages, `postgresql18-client` installé pour le `pg_dump` d'avant migration — un client de majeure inférieure au serveur refuserait de tourner, et `migrate.mjs` le signale sans bloquer).

- `npm start` = `node scripts/migrate.mjs && next start -p ${PORT:-3000}`. Les migrations passent **avant** que Next n'écoute.
- `HEALTHCHECK` sur `/api/health` : `200` quand la base répond, `503` sinon (`non_configuree` ou `muette`). Aucune requête n'est faite à l'import d'un module — c'est ce qui garantit que `next build` n'a besoin d'aucune variable d'environnement.
- `/api/health/routines` est la sonde « homme mort » : `500` dès qu'une routine dépasse trois fois sa cadence nominale. Une routine qui n'a jamais tourné n'est pas en panne. À brancher sur la supervision externe.
- Variables à poser dans Coolify : `DATABASE_URL`, `ADMIN_PASSWORD`, `AUTH_SECRET`, `CRON_SECRET`, `SITE_URL`, et si l'on veut s'écarter des valeurs par défaut `DEPECHE_TZ` (`Europe/Paris`), `DEPECHE_HEURE` (`18:00`), `BACKUP_DIR` (`.backups`, à monter sur un volume en production, sinon la copie de sécurité meurt avec le conteneur).
- Le conteneur embarque `content/` et `drizzle/`, lus au démarrage. Une modification du canon est donc un déploiement, pas une écriture en base.
